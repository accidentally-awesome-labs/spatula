---
phase: 19-deployment-self-host-excellence
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/backup-restore.md
  - docs/runbooks/reverse-proxy.md
  - docs/runbooks/nginx.conf
  - docs/runbooks/upgrade.md
  - .planning/ROADMAP.md
files_created:
  - docs/runbooks/backup-restore.md
  - docs/runbooks/reverse-proxy.md
  - docs/runbooks/nginx.conf
autonomous: true
requirements: [DEPLOY-05, DEPLOY-06, DEPLOY-07]
must_haves:
  truths:
    - 'backup-restore.md covers pg_dump + content-store + Redis reconciliation with time-to-restore estimates'
    - "reverse-proxy.md ships a tested nginx recipe with token-in-URL access-log masking; traefik + caddy are stub sections labeled 'not first-party tested'"
    - 'upgrade.md is extended with a version-to-version migration template'
    - 'ROADMAP.md v1.1 section notes the Helm chart as a v1.1 target (community-welcome in v1.x)'
  artifacts:
    - path: 'docs/runbooks/backup-restore.md'
      provides: 'Backup + restore procedure'
      contains: 'pg_dump'
    - path: 'docs/runbooks/reverse-proxy.md'
      provides: 'nginx tested recipe + traefik/caddy disclaimer stubs'
      contains: 'not first-party tested'
    - path: 'docs/runbooks/nginx.conf'
      provides: 'Working nginx reverse-proxy config with log masking'
      contains: 'proxy_pass'
  key_links:
    - from: 'docs/runbooks/reverse-proxy.md'
      to: 'docs/runbooks/nginx.conf'
      via: 'references the tested config + log-masking directive'
      pattern: 'nginx.conf'
    - from: 'docs/runbooks/backup-restore.md'
      to: 'tests/e2e/backup/round-trip.test.ts'
      via: 'documents the same pg_dump + content-store enumeration the test exercises'
      pattern: 'content_store|content-store'
---

<objective>
Write the runbook set (DEPLOY-05/06/07): `backup-restore.md` (pg_dump + content-store + Redis reconciliation, time-to-restore estimates), `reverse-proxy.md` + a tested `nginx.conf` (token-in-URL access-log masking verified), and extend the existing `upgrade.md` with a version-to-version migration template. Traefik/caddy ship as disclaimer stubs ("not first-party tested"). Also add the ROADMAP v1.1 Helm-chart note (in scope per CONTEXT deferred section).

Purpose: The committed-and-exercised runbook set for SC#5.
Output: backup-restore.md, reverse-proxy.md, nginx.conf, extended upgrade.md, ROADMAP Helm note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-deployment-self-host-excellence/19-CONTEXT.md
@.planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md
@docs/runbooks/upgrade.md

<interfaces>
<!-- Backup mechanics + nginx pattern. Extracted 2026-06-10. -->

Backup enumeration (matches Plan 06 test): content_store table has columns key, content, binary_content. No ContentStore.listKeys() — enumerate via SQL/Drizzle. Postgres-backed store is the v1 backup scope; S3 uses native bucket tooling.
Redis (BullMQ) is reconstructible queue state — document "Redis is reconcilable, not a source of truth: jobs in-flight at backup time are replayed/re-enqueued; do not treat Redis as authoritative for durable data."
API serves SSE/WS with ?token= in the URL — nginx access logs would leak tokens; mask them.
upgrade.md already has: no-migration-downgrade, expand-contract-only, two-journal model, pre-Wave-6 dev DB handling, schema-equivalence gate. DEPLOY-06 ADDS a version-to-version migration template section (do not duplicate existing policy).
ROADMAP.md v1.1 section: add the Helm note (CONTEXT deferred: "Helm chart — community-contributed welcome in v1.x; first-party targeted for v1.1").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: backup-restore.md + extend upgrade.md with version-to-version template</name>
  <read_first>
    - docs/runbooks/upgrade.md (FULL — you APPEND a new section; do not duplicate existing policy)
    - packages/db/src/schema/content.ts (content_store columns for the enumeration doc)
    - .planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md § Pattern 9 (backup steps the runbook documents), § ContentStore backup note
    - tests/e2e/backup/round-trip.test.ts IF it exists (keep the doc consistent with the test; same pg_dump + content_store enumeration)
  </read_first>
  <action>
    Create `docs/runbooks/backup-restore.md` with sections:
    1. **What to back up**: Postgres (source of truth — `pg_dump --no-owner --no-acl`), content store (Postgres-backed: included in the pg_dump since content_store is a table; S3-backed: use native bucket replication/versioning — out of first-party scope, link the provider's tooling), Redis (BullMQ — reconcilable, NOT a source of truth; document that in-flight jobs are replayed on restart and Redis need not be backed up for durable data).
    2. **Backup procedure**: exact `pg_dump` command; note the pg_dump 14+ `\restrict`/`\unrestrict` token caveat (cross-ref upgrade.md). For content store, note it travels with the pg_dump for the Postgres-backed store; enumerate via `SELECT key FROM content_store` if doing a separate content-store export.
    3. **Restore procedure**: create fresh DB → `psql < dump` → verify row counts + content-store parity (mirror the tests/e2e/backup round-trip steps so the runbook and test agree).
    4. **Time-to-restore estimates**: a small table (e.g. <1GB ~minutes; methodology note that estimates scale with dump size + disk I/O; tell operators to measure on their hardware — methodology is Claude's discretion per CONTEXT).
    5. **Verification**: how to confirm a restore is good (row-count parity + a content-hash spot check + `spatula doctor`).

    Append to `docs/runbooks/upgrade.md` a NEW section `## Version-to-Version Migration Template` (DEPLOY-06) — do NOT touch the existing sections. The template: pre-flight (pg_dump backup), check the release notes for expand/contract steps, apply migrations via the migrate Job/image (`run-migrate.js` into `__drizzle_migrations_oss`), verify (`spatula doctor` migrations check green + smoke), rollback path (forward-only — restore from the pre-flight backup, per the existing no-downgrade policy). Reference the new `backup-restore.md` for the pre-flight backup step.

  </action>
  <verify>
    <automated>test -f docs/runbooks/backup-restore.md && grep -q "pg_dump" docs/runbooks/backup-restore.md && grep -qi "time.to.restore\|time to restore" docs/runbooks/backup-restore.md && grep -q "## Version-to-Version Migration Template" docs/runbooks/upgrade.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/runbooks/backup-restore.md`
    - `grep -q "pg_dump" docs/runbooks/backup-restore.md` succeeds
    - `grep -qi "content_store\|content-store\|content store" docs/runbooks/backup-restore.md` succeeds
    - `grep -qi "redis" docs/runbooks/backup-restore.md` AND it describes Redis as reconcilable / not a source of truth
    - `grep -qi "time.to.restore\|time to restore" docs/runbooks/backup-restore.md` succeeds (estimates section)
    - `grep -q "## Version-to-Version Migration Template" docs/runbooks/upgrade.md` succeeds (DEPLOY-06 section added)
    - the existing upgrade.md sections are intact (verify: `grep -q "No-migration-downgrade policy" docs/runbooks/upgrade.md` still succeeds)
  </acceptance_criteria>
  <done>backup-restore.md covers PG+content-store+Redis with time-to-restore; upgrade.md gains the version-to-version template without disturbing existing policy.</done>
</task>

<task type="auto">
  <name>Task 2: reverse-proxy.md + tested nginx.conf (token log-masking) + traefik/caddy stubs + ROADMAP Helm note</name>
  <read_first>
    - apps/api/src/server.ts (confirms ?token= in SSE/WS URLs — the thing nginx logs must mask)
    - docs/runbooks/upgrade.md (sibling runbook tone)
    - .planning/ROADMAP.md (v1.1 section — where the Helm note goes)
    - .planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md § Pattern (nginx config + token-in-URL log masking), § Deferred (traefik/caddy stubs)
  </read_first>
  <action>
    Create `docs/runbooks/nginx.conf` — a real, `nginx -t`-valid reverse-proxy config that:
    - upstreams to the spatula api (`upstream spatula_api { server 127.0.0.1:3000; }`),
    - `proxy_pass http://spatula_api;` with the standard proxy headers (Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto),
    - SSE/WebSocket support: `proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection $connection_upgrade;` (define the `$connection_upgrade` map), `proxy_buffering off;` for `/api/v1/jobs/*/events` and `/ws/`,
    - **token-in-URL access-log masking**: define a `log_format` that masks the `?token=` query param (e.g. a `map $request $masked_request { ~^(?<prefix>.*?)token=[^&\s]+(?<suffix>.*)$ "$prefix" "token=***" "$suffix"; default $request; }` style masking — or use `$uri` instead of `$request` in a custom log_format so the query string with the token is never logged). Pick the approach that `nginx -t` accepts and that demonstrably keeps `token=` values out of access logs.
    Add comments marking the masking directive clearly.

    Create `docs/runbooks/reverse-proxy.md`:
    - **nginx (tested)** section: link/inline the nginx.conf, explain SSE/WS proxy settings, and the access-log token masking — state it was nginx-tested end-to-end with token masking verified in access logs (SC#5). Include the `nginx -t` validation command and a curl smoke (`curl -f http://localhost/health`).
    - **traefik (not first-party tested)** stub: a short labels/config sketch with a prominent "⚠️ Not first-party tested — community contributions welcome" disclaimer.
    - **caddy (not first-party tested)** stub: a short Caddyfile sketch with the same disclaimer.

    Add to `.planning/ROADMAP.md` v1.1 section a Helm note (CONTEXT deferred, in-scope-as-a-note): "Helm chart — community-contributed welcome in v1.x; first-party targeted for v1.1. kustomize is the supported k8s path at v1." Place it near the Phase 19 detail or the v1.1 overview.

  </action>
  <verify>
    <automated>test -f docs/runbooks/nginx.conf && test -f docs/runbooks/reverse-proxy.md && grep -q "proxy_pass" docs/runbooks/nginx.conf && grep -qi "token" docs/runbooks/nginx.conf && grep -q "not first-party tested" docs/runbooks/reverse-proxy.md && grep -qi "helm" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/runbooks/nginx.conf` AND `grep -q "proxy_pass" docs/runbooks/nginx.conf`
    - `grep -qi "token" docs/runbooks/nginx.conf` (token-in-URL masking present in the log config)
    - `grep -q "proxy_set_header Upgrade" docs/runbooks/nginx.conf` (SSE/WS support)
    - `nginx -t -c "$PWD/docs/runbooks/nginx.conf"` is the intended validation — if nginx is not installed in the executor env, the acceptance is `grep -q "log_format" docs/runbooks/nginx.conf` AND the masking note present (document that `nginx -t` must pass on a host with nginx)
    - `test -f docs/runbooks/reverse-proxy.md` AND `grep -q "not first-party tested" docs/runbooks/reverse-proxy.md` (traefik + caddy disclaimer)
    - `grep -qi "nginx" docs/runbooks/reverse-proxy.md` AND `grep -qi "traefik" docs/runbooks/reverse-proxy.md` AND `grep -qi "caddy" docs/runbooks/reverse-proxy.md`
    - `grep -qi "helm" .planning/ROADMAP.md` succeeds (v1.1 Helm note added)
  </acceptance_criteria>
  <done>reverse-proxy.md ships a tested nginx recipe with token log-masking + traefik/caddy "not first-party tested" stubs; nginx.conf is config-valid; ROADMAP gains the Helm v1.1 note.</done>
</task>

</tasks>

<verification>
- backup-restore.md (PG+content-store+Redis+time-to-restore) and the upgrade.md version-template section exist.
- nginx.conf is a valid reverse-proxy config with SSE/WS support and token-in-URL access-log masking; reverse-proxy.md documents nginx (tested) + traefik/caddy (not first-party tested).
- ROADMAP.md has the Helm v1.1 note.
- NOTE (manual, SC#5): the end-to-end nginx token-masking-in-access-logs verification requires a running nginx + a live token request — document it; the executor should run `nginx -t` where nginx is available.
</verification>

<success_criteria>
DEPLOY-05/06/07 runbook deliverables committed: backup-restore, version-to-version upgrade template, and the nginx-tested reverse-proxy recipe with traefik/caddy disclaimer stubs; ROADMAP Helm note added.
</success_criteria>

<output>
After completion, create `.planning/phases/19-deployment-self-host-excellence/19-08-SUMMARY.md`
</output>

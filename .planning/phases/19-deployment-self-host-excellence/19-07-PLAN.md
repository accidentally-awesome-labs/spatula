---
phase: 19-deployment-self-host-excellence
plan: 07
type: execute
wave: 3
depends_on: [19-06]
files_modified:
  - docs/support-matrix.md
  - .github/workflows/support-matrix.yml
files_created:
  - docs/support-matrix.md
  - .github/workflows/support-matrix.yml
autonomous: true
requirements: [DEPLOY-08, DEPLOY-05, DEPLOY-10, DEPLOY-11]
must_haves:
  truths:
    - "docs/support-matrix.md documents min versions: Node 22+, Postgres 14+, Redis 7+, macOS/Linux/WSL"
    - "A min-version CI matrix runs the suite against Node 22 x Postgres 14/15/16 x Redis 7 and is green"
    - "The heavy DB-dependent lanes (backup/upgrade) run on-release + nightly, not on every PR"
    - "The pure config-compat lane may run on PR (no DB needed)"
  artifacts:
    - path: "docs/support-matrix.md"
      provides: "Min-version support matrix"
      contains: "Postgres 14"
    - path: ".github/workflows/support-matrix.yml"
      provides: "Min-version CI matrix + heavy-test cadence"
      contains: "matrix"
  key_links:
    - from: ".github/workflows/support-matrix.yml"
      to: "tests/upgrade + tests/e2e/backup + tests/config"
      via: "runs pnpm test:upgrade / test:backup / test:config against service containers"
      pattern: "test:upgrade|test:config|test:backup"
---

<objective>
Ship `docs/support-matrix.md` (min versions: Node 22+, Postgres 14+, Redis 7+, macOS/Linux/WSL) and a `.github/workflows/support-matrix.yml` min-version CI matrix that runs the Phase 19 heavy test lanes (backup/upgrade) against Node 22 × Postgres 14/15/16 × Redis 7 service containers — on-release + nightly (Claude's-discretion cadence), keeping the standard PR gate light. The pure-in-process config-compat lane may also run on PR.

Purpose: DEPLOY-08 (documented + CI-enforced min versions) and the CI home for the Plan 06 heavy test lanes (DEPLOY-05/10/11 cadence).
Output: docs/support-matrix.md + .github/workflows/support-matrix.yml.

Depends on Plan 06 (the test:backup / test:upgrade / test:config scripts must exist for the workflow to invoke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-deployment-self-host-excellence/19-CONTEXT.md
@.planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md

<interfaces>
<!-- CI patterns + scripts. Extracted 2026-06-10. -->

Existing workflows: ci.yml, audit.yml, adversarial-llm.yml (the on-release+nightly heavy-lane precedent), release.yml.
adversarial-llm.yml cadence model: `on: { push: { tags: ['v*'] }, schedule: [{ cron: '...' }], workflow_dispatch: {} }` — NOT on pull_request.

Plan 06 root package.json scripts (must exist):
  test:backup  -> tests/e2e/backup/round-trip.test.ts
  test:upgrade -> tests/upgrade/vitest.config.ts
  test:config  -> tests/config/vitest.config.ts

DB the tests expect: DATABASE_URL=postgresql://postgres:postgres@localhost:5432/spatula_test (default), REDIS_URL=redis://localhost:6379.
GitHub Actions service-container pattern (from existing ci.yml) provides postgres + redis as services with health checks.

Support matrix targets (CONTEXT + ROADMAP SC#5): Node 22+, Postgres 14+, Redis 7+, OS macOS/Linux/WSL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/support-matrix.md</name>
  <read_first>
    - docs/runbooks/upgrade.md (sibling runbook tone; it references support-matrix.md as a Phase 19 deliverable)
    - .env.example (any version-relevant env hints)
    - .planning/phases/19-deployment-self-host-excellence/19-CONTEXT.md (the exact min-version targets)
  </read_first>
  <action>
    Create `docs/support-matrix.md`. Include a table with columns Component | Minimum | Tested | Notes covering at minimum:
    - Node.js: 22 (LTS) minimum; note `engines: { node: ">=22" }` in package manifests.
    - Postgres: 14 minimum; tested 14/15/16; note the pg_dump 14+ `\restrict`/`\unrestrict` normalizer caveat (referenced from upgrade.md) where relevant.
    - Redis: 7 minimum.
    - OS: macOS, Linux, WSL (note: native Windows shell is NOT supported — WSL only, per REQUIREMENTS Out-of-Scope).
    - Package manager: pnpm 9.
    Add a short "How this is enforced" section pointing at `.github/workflows/support-matrix.yml` (the min-version CI matrix) and noting that the matrix runs on-release + nightly. Add a "Container images" note: api/worker/migrate run on distroless (no shell); cli is Debian-slim; Playwright browsers are installed on the host via `spatula setup`, not baked into the cli image (cross-reference Plan 02 decision).
  </action>
  <verify>
    <automated>test -f docs/support-matrix.md && grep -q "Postgres 14" docs/support-matrix.md && grep -q "Node 22" docs/support-matrix.md && grep -q "Redis 7" docs/support-matrix.md && grep -qi "WSL" docs/support-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/support-matrix.md`
    - `grep -q "Postgres 14" docs/support-matrix.md` succeeds
    - `grep -q "Node 22" docs/support-matrix.md` succeeds
    - `grep -q "Redis 7" docs/support-matrix.md` succeeds
    - `grep -qi "WSL" docs/support-matrix.md` AND `grep -qi "macOS" docs/support-matrix.md` AND `grep -qi "Linux" docs/support-matrix.md`
    - `grep -q "support-matrix.yml" docs/support-matrix.md` (cross-references the CI enforcement)
  </acceptance_criteria>
  <done>support-matrix.md documents Node 22+/PG 14+/Redis 7+/macOS-Linux-WSL with the enforcement + container-image notes.</done>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/support-matrix.yml (min-version matrix + heavy-test cadence)</name>
  <read_first>
    - .github/workflows/ci.yml (existing postgres/redis service-container pattern + pnpm/node setup steps to reuse)
    - .github/workflows/adversarial-llm.yml (the on-release + nightly + workflow_dispatch cadence to mirror)
    - package.json (test:backup / test:upgrade / test:config scripts from Plan 06)
    - .planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md § Heavy Test CI Cadence, § Phase Requirements → Test Map (DEPLOY-08 matrix command)
  </read_first>
  <action>
    Create `.github/workflows/support-matrix.yml`:
    - Triggers (cadence, RESEARCH § Heavy Test CI Cadence — NOT pull_request):
      ```yaml
      on:
        push: { tags: ['v*'] }
        schedule: [{ cron: '0 2 * * *' }]
        workflow_dispatch: {}
      ```
    - A `min-version` job with a matrix over Postgres versions:
      ```yaml
      strategy:
        fail-fast: false
        matrix:
          postgres: ['14', '15', '16']
          node: ['22']
          redis: ['7']
      services:
        postgres:
          image: postgres:${{ matrix.postgres }}-alpine
          env: { POSTGRES_USER: postgres, POSTGRES_PASSWORD: postgres, POSTGRES_DB: spatula_test }
          ports: ['5432:5432']
          options: >-
            --health-cmd "pg_isready -U postgres" --health-interval 5s --health-timeout 3s --health-retries 5
        redis:
          image: redis:${{ matrix.redis }}-alpine
          ports: ['6379:6379']
          options: >-
            --health-cmd "redis-cli ping" --health-interval 5s --health-timeout 3s --health-retries 5
      ```
    - Steps: checkout → pnpm/action-setup@v4 (v9) → setup-node (matrix.node) → `pnpm install --frozen-lockfile` → `pnpm build` → apply migrations (`DATABASE_URL=... node packages/db/dist/run-migrate.js` or `pnpm --filter @spatula/db ...`) → run the heavy lanes with the service-container env:
      ```yaml
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/spatula_test
        REDIS_URL: redis://localhost:6379
      run: |
        pnpm test:config
        pnpm test:upgrade
        pnpm test:backup
      ```
    - Add a comment header documenting the cadence decision: "Heavy DB lanes run on-release + nightly (Claude's-discretion, Phase 19 CONTEXT). The light config-compat lane (pnpm test:config) is safe on PR; wire it into ci.yml separately if desired — Phase 21 owns the full CI topology."
    Do NOT add these heavy lanes to ci.yml (the PR gate) — keep this phase's CI additive (CONTEXT Claude's-discretion). Full topology is Phase 21.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/support-matrix.yml')); j=d['jobs']; mv=next(iter(j.values())); m=mv['strategy']['matrix']; assert '14' in m['postgres'] and '15' in m['postgres'] and '16' in m['postgres']; assert 'pull_request' not in (d.get(True) or d.get('on') or {}); print('OK', m)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/support-matrix.yml`
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/support-matrix.yml'))"` exits 0
    - matrix includes postgres 14, 15, 16 (python assertion above)
    - workflow does NOT trigger on `pull_request` (python assertion above)
    - `grep -q "schedule" .github/workflows/support-matrix.yml` AND `grep -q "tags:" .github/workflows/support-matrix.yml` (on-release + nightly)
    - `grep -q "test:upgrade" .github/workflows/support-matrix.yml` AND `grep -q "test:config" .github/workflows/support-matrix.yml` AND `grep -q "test:backup" .github/workflows/support-matrix.yml`
    - `grep -q "postgres:" .github/workflows/support-matrix.yml` AND `grep -q "redis:" .github/workflows/support-matrix.yml` (service containers)
    - `grep -c "pull_request" .github/workflows/ci.yml` is unchanged AND the heavy lanes are NOT added to ci.yml (verify: `grep -c "test:backup" .github/workflows/ci.yml` returns 0)
  </acceptance_criteria>
  <done>support-matrix.yml runs the heavy lanes against PG 14/15/16 + Redis 7 + Node 22 on-release+nightly (not PR); ci.yml PR gate untouched.</done>
</task>

</tasks>

<verification>
- docs/support-matrix.md documents Node 22+/PG 14+/Redis 7+/macOS-Linux-WSL.
- support-matrix.yml is valid YAML, matrix over PG 14/15/16, triggers on tags + nightly + dispatch (not pull_request), runs test:config/upgrade/backup with service containers.
- ci.yml (the PR gate) is not burdened with the DB-heavy lanes.
</verification>

<success_criteria>
DEPLOY-08 satisfied: documented support matrix + a green min-version CI matrix; and the Plan 06 heavy lanes have their on-release+nightly CI home (DEPLOY-05/10/11 cadence).
</success_criteria>

<output>
After completion, create `.planning/phases/19-deployment-self-host-excellence/19-07-SUMMARY.md`
</output>

---
phase: 19-deployment-self-host-excellence
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sizing-baseline.ts
  - docs/runbooks/hardware-sizing.md
  - package.json
files_created:
  - scripts/sizing-baseline.ts
  - docs/runbooks/hardware-sizing.md
autonomous: false
requirements: [DEPLOY-09]
must_haves:
  truths:
    - 'A reproducible sizing-baseline harness runs a 1k-page crawl once per routing tier (fast/primary/smart) and records wall-clock + LLM cost-per-page (D-01/02/03)'
    - 'The harness is gated behind SPATULA_LIVE_LLM and is manual/main-only — it never blocks fork CI'
    - 'hardware-sizing.md names one defined cloud VM class and contains a MEASURED 1k-page baseline table (one row per tier), not synthetic estimates'
    - 'The harness + assumptions are documented so self-hosters can re-run for their own hardware'
  artifacts:
    - path: 'scripts/sizing-baseline.ts'
      provides: '1k-page-per-tier live measurement harness'
      contains: 'SPATULA_LIVE_LLM'
    - path: 'docs/runbooks/hardware-sizing.md'
      provides: 'Measured baseline table + named VM + re-run instructions'
      contains: '1000 pages'
  key_links:
    - from: 'scripts/sizing-baseline.ts'
      to: 'live crawl + usage/cost API'
      via: 'runs a crawl job and reads per-page LLM cost per tier'
      pattern: 'tier|cost'
    - from: 'docs/runbooks/hardware-sizing.md'
      to: 'scripts/sizing-baseline.ts'
      via: 'documents the harness command + assumptions for self-host re-run'
      pattern: 'sizing-baseline'
---

<objective>
Build the measured hardware-sizing baseline (DEPLOY-09, D-01/02/03): a reproducible harness that runs a 1k-page crawl ONCE PER routing tier (fast/primary/smart) on ONE defined cloud VM, recording wall-clock and LLM cost-per-page, then a `hardware-sizing.md` runbook with the MEASURED table (one row per tier), the named VM, and re-run instructions. The live measurement (real LLM spend) is gated behind `SPATULA_LIVE_LLM` and is manual/main-only — it never blocks fork CI (CONTEXT live-gating rule).

Purpose: A genuinely measured sizing table (the user explicitly chose full live measurement over synthetic estimates) for SC#5.
Output: scripts/sizing-baseline.ts harness + docs/runbooks/hardware-sizing.md (table filled by the live run via the checkpoint).

This plan has a checkpoint:human-action — the live 3×1k-page run incurs real LLM cost + wall-clock on a real cloud VM and cannot be automated in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-deployment-self-host-excellence/19-CONTEXT.md
@.planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md

<interfaces>
<!-- Live-gate + cost surfaces. Extracted 2026-06-10. -->

SPATULA_LIVE_LLM gate: `it.skipIf(LIVE)` pattern; default test runs exclude live-LLM; fork CI passes without OPENROUTER_API_KEY. The sizing harness MUST refuse to run (or no-op with a message) unless SPATULA_LIVE_LLM=1 is set, so it never accidentally spends in CI.

Routing tiers: fast / primary / smart (smart-routing model tiers — one cost row each, D-02).

Cost surface: the project already has a usage/cost API (RESEARCH § DEPLOY-09: "existing usage/cost API reusable"). Read the actual usage-cost surface (apps/api usage-recorder / admin metrics) to get per-page LLM cost. Grep for it during read_first.

VM: ONE defined cloud VM class, named explicitly (e.g. ~4 vCPU / 8 GB / SSD — pick a representative reproducible instance type, D-01). A laptop is rejected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/sizing-baseline.ts harness (1k pages per tier, live-gated) + runbook skeleton</name>
  <read_first>
    - apps/api/src/services/usage-recorder.ts (the per-job LLM cost surface to read cost-per-page from)
    - .github/workflows/adversarial-llm.yml (the SPATULA_LIVE_LLM live-gate precedent)
    - scripts/ (existing scripts conventions — how project scripts are structured/run)
    - packages/core/src (routing tiers fast/primary/smart — confirm the tier identifiers)
    - .planning/phases/19-deployment-self-host-excellence/19-CONTEXT.md D-01/02/03 (measurement constraints)
  </read_first>
  <action>
    Create `scripts/sizing-baseline.ts` — a runnable harness (tsx) that:
    1. **Refuses to run without the live gate**: at the top, `if (process.env.SPATULA_LIVE_LLM !== '1') { console.error('Refusing to run: set SPATULA_LIVE_LLM=1 (this harness incurs real LLM cost).'); process.exit(2); }`. This guarantees it never spends in fork CI.
    2. **Per-tier loop** over `['fast','primary','smart']` (confirm exact identifiers from core during read): for each tier, run/trigger a 1000-page crawl job (use the project's crawl entrypoint / a seed-URL list of ~1000 pages; allow the seed source + page count to be parameterized via env/args, default 1000), pinning the routing to that tier.
    3. **Measure**: record wall-clock (start/end `performance.now()` or Date) for the 1000-page run, and read total LLM cost from the usage/cost surface (usage-recorder/admin metrics) → compute cost-per-page = total_cost / pages_completed.
    4. **Emit**: print a markdown table row per tier (tier | pages | wall-clock | total LLM cost | cost/page) to stdout AND write a machine-readable JSON to `scripts/.sizing-results.json` (gitignored or a results artifact) so the runbook table can be filled from it.
    5. **Document assumptions in-file**: header comment naming the target VM class, the crawler used, the model per tier, and that results vary by hardware/network/site.
    Add a root `package.json` script: `"sizing:baseline": "SPATULA_LIVE_LLM=1 tsx scripts/sizing-baseline.ts"` (keep the env in the script so it's explicit, or document the env requirement in the runbook).

    Create `docs/runbooks/hardware-sizing.md` SKELETON now (table filled by the checkpoint live run in Task 2):
    - **Defined hardware**: name the ONE cloud VM class explicitly (D-01) — e.g. "Measured on a {provider} {instance-type} (4 vCPU / 8 GB RAM / SSD)". A laptop is explicitly rejected as a server proxy.
    - **Methodology**: 1000-page crawl, run once per routing tier (D-03 full live), via `scripts/sizing-baseline.ts`. Document the seed corpus + crawler + model-per-tier assumptions.
    - **Measured baseline table** (one row per tier, fast/primary/smart): columns Tier | Pages | Wall-clock | Total LLM cost | LLM cost/page. Leave the numeric cells as `<filled by live run>` placeholders for now.
    - **Re-run instructions**: how a self-hoster runs `pnpm sizing:baseline` (with SPATULA_LIVE_LLM=1 + an OPENROUTER_API_KEY) on their own hardware and interprets results.

  </action>
  <verify>
    <automated>test -f scripts/sizing-baseline.ts && grep -q "SPATULA_LIVE_LLM" scripts/sizing-baseline.ts && grep -q "fast\|primary\|smart" scripts/sizing-baseline.ts && test -f docs/runbooks/hardware-sizing.md && grep -q "1000 pages\|1k-page\|1000-page" docs/runbooks/hardware-sizing.md && pnpm exec tsc --noEmit scripts/sizing-baseline.ts 2>/dev/null || echo "typecheck-note: run via tsx at runtime"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/sizing-baseline.ts`
    - `grep -q "SPATULA_LIVE_LLM" scripts/sizing-baseline.ts` succeeds AND the harness exits non-zero when the gate is unset (verify: `SPATULA_LIVE_LLM= tsx scripts/sizing-baseline.ts; echo $?` is non-zero)
    - `grep -q "fast" scripts/sizing-baseline.ts && grep -q "primary" scripts/sizing-baseline.ts && grep -q "smart" scripts/sizing-baseline.ts` (all three tiers, D-02)
    - `grep -qi "cost" scripts/sizing-baseline.ts AND grep -qi "page" scripts/sizing-baseline.ts` (cost-per-page measurement)
    - `test -f docs/runbooks/hardware-sizing.md` AND `grep -qi "1000\|1k" docs/runbooks/hardware-sizing.md`
    - `grep -qi "vCPU\|instance\|VM" docs/runbooks/hardware-sizing.md` (named cloud VM, D-01)
    - `grep -q "sizing:baseline\|sizing-baseline" package.json` (run script added)
  </acceptance_criteria>
  <done>Live-gated 1k-page-per-tier harness exists; refuses to run without SPATULA_LIVE_LLM; hardware-sizing.md skeleton names the VM, documents methodology, has the per-tier table with placeholders + re-run instructions.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human-action — run the live 1k-page-per-tier sizing measurement (DEPLOY-09)</name>
  <action>Human runs the live, paid 3×1k-page measurement on the defined cloud VM and fills the hardware-sizing.md table — see how-to-verify. Cannot be automated (real LLM spend + real VM; live-gated out of CI).</action>
  <what-built>scripts/sizing-baseline.ts (live-gated 1k-page-per-tier harness) + docs/runbooks/hardware-sizing.md skeleton with placeholder table cells. The measured numbers require a real, paid, live run on the defined cloud VM (D-03 full live measurement — real LLM spend + crawl wall-clock).</what-built>
  <how-to-verify>
    This is genuinely manual (real cost + a real cloud VM; CONTEXT live-gating means it must never run in fork CI). Steps:
    1. Provision the named cloud VM (the instance class documented in hardware-sizing.md — ~4 vCPU / 8 GB / SSD or whatever the runbook names).
    2. Export OPENROUTER_API_KEY and set SPATULA_LIVE_LLM=1.
    3. Run `pnpm sizing:baseline` — it executes the 1000-page crawl ONCE PER tier (3 live runs total: fast, primary, smart). Expect real LLM charges and meaningful wall-clock.
    4. Take the emitted per-tier table rows (and/or scripts/.sizing-results.json) and FILL the `<filled by live run>` placeholders in docs/runbooks/hardware-sizing.md with the measured wall-clock + cost/page per tier.
    5. Commit the filled table.
    Report: the three measured rows (tier, wall-clock, cost/page) and the VM you used.
  </how-to-verify>
  <resume-signal>Type "approved" with the three measured rows pasted (and confirm hardware-sizing.md placeholders are filled), or describe a blocker (no VM budget, LLM-key issue, harness failure).</resume-signal>
</task>

</tasks>

<verification>
- scripts/sizing-baseline.ts exists, is live-gated (exits non-zero without SPATULA_LIVE_LLM=1), loops all three tiers, measures cost/page.
- docs/runbooks/hardware-sizing.md names one VM, documents the 1k-page-per-tier methodology, and has the per-tier table + re-run instructions.
- The MEASURED numbers are filled via the human-action checkpoint (DEPLOY-09 requires a real live run — VALIDATION.md marks this manual-only).
</verification>

<success_criteria>
DEPLOY-09 satisfied: a reproducible, live-gated 1k-page-per-tier harness and a hardware-sizing.md whose baseline table is genuinely measured (post-checkpoint) on one named cloud VM, with self-host re-run instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/19-deployment-self-host-excellence/19-09-SUMMARY.md`
</output>

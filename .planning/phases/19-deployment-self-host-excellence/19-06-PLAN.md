---
phase: 19-deployment-self-host-excellence
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/backup/round-trip.test.ts
  - tests/upgrade/migrate-and-verify.test.ts
  - tests/upgrade/vitest.config.ts
  - tests/config/config-compat.test.ts
  - tests/config/vitest.config.ts
  - tests/config/fixtures/v1.0-spatula.yaml
  - package.json
autonomous: true
requirements: [DEPLOY-05, DEPLOY-10, DEPLOY-11]
must_haves:
  truths:
    - "tests/e2e/backup/ performs pg_dump + content-store snapshot, restores into a fresh DB, and asserts row-count + content-hash parity"
    - "tests/upgrade/ seeds a v1.0 baseline DB, applies v1.x migrations, and verifies the runtime/journal cleanly"
    - "tests/config/ parses a v1.0 spatula.yaml on the v1.1 runtime without throwing"
    - "Each suite skips gracefully when its Postgres dependency is absent (config suite needs no DB)"
  artifacts:
    - path: "tests/e2e/backup/round-trip.test.ts"
      provides: "Backup -> restore round-trip with parity assertions"
      contains: "pg_dump"
    - path: "tests/upgrade/migrate-and-verify.test.ts"
      provides: "v1.0-seed -> v1.x-migrate -> verify"
      contains: "__drizzle_migrations_oss"
    - path: "tests/config/config-compat.test.ts"
      provides: "v1.0 spatula.yaml parses on v1.1 runtime"
      contains: "parseProjectYamlFile"
  key_links:
    - from: "tests/e2e/backup/round-trip.test.ts"
      to: "content_store table"
      via: "Drizzle SELECT key/content (no listKeys) then retrieve() parity"
      pattern: "content_store"
    - from: "tests/config/config-compat.test.ts"
      to: "@spatula/core parseProjectYamlFile"
      via: "import + call on the fixture"
      pattern: "parseProjectYamlFile"
---

<objective>
Build the three operational-assurance test harnesses (DEPLOY-05/10/11): a backup→restore round-trip with row-count + content-hash parity, a v1.0→v1.x upgrade migration test, and a v1.0 `spatula.yaml` config-compat test. These follow the existing root-`tests/` conventions (DSR round-trip pattern: direct DB/ContentStore calls, skip-if-no-DB gating; Node-builtin fixtures).

Purpose: Prove backup, upgrade, and config-compat work — the core of SC#4.
Output: tests/e2e/backup/, tests/upgrade/, tests/config/ + their configs + root package.json scripts.

CI cadence (Claude's discretion, RESEARCH § Heavy Test CI Cadence): these DB-heavy lanes run on-release + nightly (NOT every PR); the pure-in-process config-compat test may also run on PR. The CI wiring for cadence is in Plan 07 (support-matrix workflow) — this plan ships the tests + scripts only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-deployment-self-host-excellence/19-CONTEXT.md
@.planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md
@tests/e2e/dsr/deletion/round-trip.test.ts

<interfaces>
<!-- Exact contracts. Extracted 2026-06-10. -->

ContentStore interface (packages/core/src/interfaces/content-store.ts) — NO listKeys():
```typescript
export interface ContentStore {
  store(key: string, content: string): Promise<string>;
  retrieve(ref: string): Promise<string>;
  delete(ref: string): Promise<void>;
  storeBinary(key: string, data: Uint8Array): Promise<string>;
  retrieveBinary(ref: string): Promise<Uint8Array | null>;
  getDownloadUrl?(ref: string, expiresInSeconds?: number): Promise<string>;
}
```

content_store table (packages/db/src/schema/content.ts) — enumerate via Drizzle (no listKeys):
```typescript
export const contentStore = pgTable('content_store', {
  id: uuid('id').primaryKey().defaultRandom(),
  key: text('key').notNull().unique(),
  content: text('content'),            // nullable — text exports
  binaryContent: bytea('binary_content'), // nullable — binary exports
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
});
```

From @spatula/db barrel: createDatabase(url), PgContentStore, TenantDataRepository. (DSR test imports these.)
DB skip pattern (from DSR test): try createDatabase(DATABASE_URL) + `SELECT 1`; if it throws, set setupOk=false and skip.
DATABASE_URL default in tests: 'postgresql://postgres:postgres@localhost:5432/spatula_test'.

From @spatula/core: parseProjectYamlFile(filePath) and parseProjectYaml(content) in packages/core/src/config/yaml-parser.ts — throw on invalid; return SpatulaYaml on success.

Migration runner: packages/db/dist/run-migrate.js applies packages/db/drizzle/ into __drizzle_migrations_oss. Baseline migration: packages/db/drizzle/0000_v1_baseline.sql. Incremental: 0001_api_key_rotation.sql.

Root vitest config (tests/vitest.config.ts) already includes 'tests/e2e/**/*.test.ts' — backup test is auto-picked-up. upgrade + config need sibling configs (RESEARCH § Vitest Config).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/e2e/backup/round-trip.test.ts (pg_dump + content-store parity)</name>
  <read_first>
    - tests/e2e/dsr/deletion/round-trip.test.ts (the EXACT pattern to follow: DB connect, skip-if-no-DB, direct calls)
    - tests/vitest.config.ts (root config — already includes tests/e2e/**; backup auto-discovered)
    - packages/db/src/schema/content.ts (content_store columns)
    - packages/core/src/interfaces/content-store.ts (no listKeys — enumerate the table)
    - .planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md § Pattern 9 (backup round-trip steps), § ContentStore backup note
  </read_first>
  <behavior>
    - Test: After seeding rows in several tables + N content_store blobs, pg_dump → restore into a fresh DB → row counts per table match the originals AND each content_store blob's content hash matches (parity).
    - Edge: when DATABASE_URL Postgres is unavailable, the suite skips (does not fail).
  </behavior>
  <action>
    Create `tests/e2e/backup/round-trip.test.ts` following the DSR deletion test structure (RESEARCH § Pattern 9):
    1. `beforeAll`: `createDatabase(DATABASE_URL)`, `SELECT 1` connectivity check; set `setupOk`; skip all `it` via `it.skipIf(!setupOk)` when DB is down.
    2. Seed: insert a handful of rows across a few tenant-scoped tables (reuse seed helpers if available, else minimal inserts) and write 2–3 blobs via `PgContentStore.store()`. Record original per-table counts and the SHA-256 of each blob's content.
    3. Capture: `pg_dump --no-owner --no-acl` of the test DB via `execFileSync('pg_dump', [...])` into a buffer/temp file. Enumerate content_store via Drizzle `SELECT key, content FROM content_store` (NO listKeys — RESEARCH note) and snapshot it.
    4. Restore: into a fresh scratch DB (create a uniquely-named DB or DROP+CREATE a `spatula_backup_restore_test` DB), `psql < dump` to restore schema+data; content_store rows come back with the dump (they're in the same DB), so the content-store snapshot parity is verified by re-reading via Drizzle/`retrieve()` against the restored DB.
    5. Assert: `SELECT COUNT(*)` per seeded table on the restored DB equals the originals; each restored blob's SHA-256 equals the recorded hash (content-hash parity).
    6. Cleanup in `afterAll`: drop the scratch DB; close pools.
    Scope note (RESEARCH): Postgres-backed ContentStore only for v1 backup; S3/Local stores are out of scope (the runbook in Plan 08 documents that). Use `node:crypto` createHash('sha256') for the content hash. Gate on real Postgres (skip if absent), exactly like the DSR test.
  </action>
  <verify>
    <automated>pnpm exec vitest run --config tests/vitest.config.ts tests/e2e/backup/round-trip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/e2e/backup/round-trip.test.ts`
    - `grep -q "pg_dump" tests/e2e/backup/round-trip.test.ts` succeeds
    - `grep -q "content_store\|contentStore" tests/e2e/backup/round-trip.test.ts` succeeds (table enumerated, no listKeys)
    - `grep -q "createHash\|sha256\|SHA-256\|sha-256" tests/e2e/backup/round-trip.test.ts` succeeds (content-hash parity)
    - `grep -q "skipIf\|setupOk" tests/e2e/backup/round-trip.test.ts` succeeds (skip-if-no-DB gating)
    - `pnpm exec vitest run --config tests/vitest.config.ts tests/e2e/backup/round-trip.test.ts` exits 0 (passes with a real DB, or skips cleanly without one)
  </acceptance_criteria>
  <done>Backup round-trip test seeds → pg_dumps → restores → asserts row-count + content-hash parity; skips gracefully without Postgres.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/upgrade/ (v1.0 baseline → v1.x migrate → verify) + tests/config/ (yaml compat) + scripts</name>
  <read_first>
    - packages/db/drizzle/0000_v1_baseline.sql (the "v1.0 DB" seed)
    - packages/db/drizzle/0001_api_key_rotation.sql (the v1.x increment applied)
    - packages/db/src/run-migrate.ts (migration runner + __drizzle_migrations_oss journal)
    - packages/core/src/config/yaml-parser.ts (parseProjectYamlFile / parseProjectYaml exports)
    - tests/e2e/dsr/deletion/round-trip.test.ts (skip-if-no-DB pattern for the upgrade test)
    - .planning/phases/19-deployment-self-host-excellence/19-RESEARCH.md § Pattern 9 (upgrade + config-compat steps), § Vitest Config
  </read_first>
  <behavior>
    - Upgrade test: applying 0000_v1_baseline.sql to a fresh DB (simulating v1.0), then running the migrator, leaves __drizzle_migrations_oss with the expected applied migrations and every expected table present (schema smoke). Skips without Postgres.
    - Config test (no DB): a v1.0 spatula.yaml fixture parses on the v1.1 runtime via parseProjectYamlFile without throwing, and required fields parse correctly.
  </behavior>
  <action>
    Create `tests/upgrade/vitest.config.ts` (RESEARCH § Vitest Config): node env, `include: ['tests/upgrade/**/*.test.ts']`, `passWithNoTests: true`, and the `@spatula/*` src aliases (db/core/queue/shared) mirroring the root config.

    Create `tests/upgrade/migrate-and-verify.test.ts` (RESEARCH § Pattern 9 upgrade):
    1. Skip-if-no-DB gate (DSR pattern).
    2. Create a fresh scratch DB; apply `0000_v1_baseline.sql` directly via `psql`/raw SQL execution (simulates a v1.0 DB — apply the baseline WITHOUT the journal so the migrator sees it as un-tracked, OR follow the realistic path: run the migrator from empty and assert it applies baseline + increments — choose the approach that proves "v1.0 DB upgrades cleanly"; document which in a comment). The intent is to prove the expand-contract upgrade path works.
    3. Run the migrator (`run-migrate` logic, pointed at the scratch DB) to apply v1.x increments.
    4. Assert `__drizzle_migrations_oss` contains the expected applied migration rows and `SELECT 1` against each expected table succeeds (schema-level smoke; no data seeding needed).
    5. Cleanup: drop scratch DB; close pool.

    Create `tests/config/vitest.config.ts`: node env, `include: ['tests/config/**/*.test.ts']`, `passWithNoTests: true`, `@spatula/core` + `@spatula/shared` aliases.

    Create `tests/config/fixtures/v1.0-spatula.yaml`: a representative v1.0 project config (a realistic minimal `spatula.yaml` with the v1.0 shape — entities/fields/seeds per the yaml-parser's SpatulaYaml schema). Read yaml-parser.ts to get the exact required keys.

    Create `tests/config/config-compat.test.ts` (RESEARCH § Pattern 9 config — pure in-process, no DB): import `parseProjectYamlFile` from `@spatula/core`, call it on the fixture path, assert no throw and that required fields parsed (e.g. project name, at least one entity/field). No DB, no HTTP.

    Add to root `package.json` scripts:
    ```json
    "test:backup": "vitest run --config tests/vitest.config.ts tests/e2e/backup/round-trip.test.ts",
    "test:upgrade": "vitest run --config tests/upgrade/vitest.config.ts",
    "test:config": "vitest run --config tests/config/vitest.config.ts"
    ```
  </action>
  <verify>
    <automated>pnpm exec vitest run --config tests/config/vitest.config.ts && pnpm exec vitest run --config tests/upgrade/vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/upgrade/migrate-and-verify.test.ts` AND `test -f tests/upgrade/vitest.config.ts`
    - `grep -q "__drizzle_migrations_oss" tests/upgrade/migrate-and-verify.test.ts` succeeds
    - `grep -q "0000_v1_baseline\|baseline" tests/upgrade/migrate-and-verify.test.ts` succeeds (v1.0 seed)
    - `test -f tests/config/config-compat.test.ts` AND `test -f tests/config/vitest.config.ts` AND `test -f tests/config/fixtures/v1.0-spatula.yaml`
    - `grep -q "parseProjectYamlFile" tests/config/config-compat.test.ts` succeeds
    - root package.json contains `"test:upgrade"` and `"test:config"` and `"test:backup"` scripts (verify: `grep -q '"test:config"' package.json`)
    - `pnpm exec vitest run --config tests/config/vitest.config.ts` exits 0 (config test passes — no DB needed)
    - `pnpm exec vitest run --config tests/upgrade/vitest.config.ts` exits 0 (passes with DB or skips cleanly)
  </acceptance_criteria>
  <done>Upgrade test proves v1.0→v1.x migration leaves the expected journal + schema; config-compat test parses a v1.0 spatula.yaml on v1.1; sibling configs + root scripts added; config test green without a DB.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run --config tests/config/vitest.config.ts` passes with no DB (pure in-process).
- backup + upgrade suites pass against a real Postgres and skip cleanly without one.
- Root package.json has test:backup / test:upgrade / test:config scripts.
- Backup test enumerates content_store via Drizzle (no listKeys) and asserts content-hash parity.
</verification>

<success_criteria>
DEPLOY-05/10/11 test harnesses exist and pass (or skip gracefully): backup round-trip parity, v1.0→v1.x upgrade verify, and v1.0 config parse on v1.1.
</success_criteria>

<output>
After completion, create `.planning/phases/19-deployment-self-host-excellence/19-06-SUMMARY.md`
</output>

---
phase: 17-browser-auth-sse-cors
plan: 06
type: execute
wave: 3
depends_on: ['02', '05']
files_modified:
  - packages/client/package.json
  - packages/client/src/methods/get-job-events.ts
  - packages/client/src/index.ts
  - packages/client/src/methods/get-job-events.test.ts
  - tests/e2e/browser/vitest.config.ts
  - tests/e2e/browser/oidc-sse-flow.spec.ts
  - tests/e2e/browser/README.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04]
must_haves:
  truths:
    - '@spatula/client exposes a streaming subscribeJobEvents/getJobEvents method backed by SSE'
    - 'In Node the SSE method uses the eventsource polyfill; in the browser it uses native EventSource'
    - 'The SDK SSE method passes ?token= and resumes via lastEventId'
    - 'A Playwright browser test drives the full chain: OIDC login -> ws-token -> SSE subscribe -> disconnect -> reconnect with Last-Event-ID -> resume'
    - '@spatula/client stays under its 50 KB gzipped size-limit budget after adding the SSE method'
  artifacts:
    - path: 'packages/client/src/methods/get-job-events.ts'
      provides: 'Real SSE streaming implementation replacing the Phase 16 non-streaming stub'
      contains: 'EventSource'
    - path: 'tests/e2e/browser/oidc-sse-flow.spec.ts'
      provides: 'Playwright e2e — full browser OIDC + SSE reconnect chain (AUTH-01/02/04 success criterion 1)'
      contains: 'Last-Event-ID'
  key_links:
    - from: 'packages/client/src/methods/get-job-events.ts'
      to: 'GET /api/v1/jobs/:id/events'
      via: 'EventSource connection with ?token= query param'
      pattern: 'events'
    - from: 'tests/e2e/browser/oidc-sse-flow.spec.ts'
      to: 'examples/auth-dex/'
      via: 'Playwright drives the Dex OIDC login then subscribes via SSE'
      pattern: 'dex'
---

<objective>
Close the SDK + browser-e2e side of Phase 17. Replace the Phase 16 non-streaming `get-job-events.ts` stub in `@spatula/client` with a real SSE streaming method (native `EventSource` in the browser, the `eventsource@4.1.0` polyfill in Node via guarded dynamic import) that passes `?token=` and resumes via `lastEventId`. Then write the Playwright browser e2e (`tests/e2e/browser/`) that drives the full chain end to end — OIDC login via the Dex kit, `POST /api/v1/ws-token`, SSE subscribe on `GET /api/v1/jobs/:id/events?token=`, mid-stream disconnect, reconnect with `Last-Event-ID`, resume from buffered events — which is ROADMAP success criterion 1.

Purpose: Web-UI enablement is hollow without an SDK method to consume the SSE stream and an e2e proof the whole reconnect chain works in a real browser.
Output: An `eventsource` dependency, a real `get-job-events.ts` SSE implementation, an SDK unit test, and the Playwright browser e2e suite + its README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md
@.planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md
@.planning/phases/17-browser-auth-sse-cors/17-02-SUMMARY.md
@.planning/phases/17-browser-auth-sse-cors/17-05-SUMMARY.md

<interfaces>
<!-- Phase 16 stub being replaced (packages/client/src/methods/get-job-events.ts) -->
```typescript
export interface JobEvent { id: string; type: string; data: Record<string, unknown>; timestamp: string; }
export async function getJobEvents(client, jobId, params): Promise<JobEvent[]>  // <-- non-streaming; replace
```

<!-- SDK SSE target shape (RESEARCH Code Examples — @spatula/client getJobEvents SSE) -->

```typescript
export function subscribeJobEvents(
  client,
  jobId,
  options: {
    onEvent: (event: JobEvent) => void;
    onError?: (err: Event) => void;
    lastEventId?: string;
    token: string; // single-use stream token from POST /api/v1/ws-token
  },
): () => void /* unsubscribe */;
```

<!-- SSE endpoint contract (from plan 17-02): GET /api/v1/jobs/:id/events?token=<t>&lastEventId=<id>
     - emits SSE frames with `id:` = redis stream id, `data:` = JSON of a JobEvent
     - emits a `replay_truncated` named event when lastEventId is too old
     - emits `:` keepalive comments every 15s -->

<!-- @spatula/client constraints: ESM-only, sideEffects:false, size-limit budget <=50 KB gzipped
     measured for `import { SpatulaClient, createJob, listJobs, getEntities }`.
     eventsource@4.1.0 is ~3 KB gzipped (RESEARCH Standard Stack) — Node-only conditional import. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the get-job-events stub with a real SSE streaming method</name>
  <files>packages/client/package.json, packages/client/src/methods/get-job-events.ts, packages/client/src/index.ts, packages/client/src/methods/get-job-events.test.ts</files>
  <read_first>
    - packages/client/src/methods/get-job-events.ts (the Phase 16 non-streaming stub being replaced)
    - packages/client/src/client.ts (SpatulaClient — baseUrl, request(), how methods attach)
    - packages/client/src/index.ts (the package barrel — exports to update)
    - packages/client/package.json (dependencies, exports field, sideEffects flag)
    - packages/client/src/methods/list-jobs.ts (an existing method — house style for a method module)
    - .planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md (Standard Stack — eventsource@4.1.0; Code Examples — subscribeJobEvents shape; Don't Hand-Roll — use the polyfill not a custom parser)
    - .planning/phases/17-browser-auth-sse-cors/17-02-SUMMARY.md (the SSE endpoint contract this consumes)
  </read_first>
  <behavior>
    - Test 1: `subscribeJobEvents` builds a URL `${baseUrl}/api/v1/jobs/${jobId}/events` with `token` as a query param
    - Test 2: when `lastEventId` is provided it is appended as a `lastEventId` query param
    - Test 3: each SSE message frame is JSON-parsed and delivered to `onEvent`
    - Test 4: a `replay_truncated` named event is delivered to a `replay_truncated` listener (or surfaced via onEvent with type discrimination)
    - Test 5: the returned unsubscribe function closes the EventSource
    - Test 6: in a Node environment (no `window`), the implementation imports `eventsource`; the import is dynamic/guarded so the browser bundle does not include it
  </behavior>
  <action>
    Add the `eventsource` dependency and rewrite `packages/client/src/methods/get-job-events.ts` as a real SSE method per RESEARCH (Standard Stack + Code Examples + Don't Hand-Roll).

    1. `packages/client/package.json` — add `"eventsource": "4.1.0"` to `dependencies`. RESEARCH confirms `eventsource@4.1.0` is MIT, ~3 KB gzipped, 1 dep — within the 50 KB budget. Because the size-limit measurement targets `import { SpatulaClient, createJob, listJobs, getEntities }` (NOT `getJobEvents`), and the `eventsource` import is dynamic + Node-guarded, the measured browser bundle should not grow — but verify (step 4).
    2. Rewrite `get-job-events.ts`. Export `subscribeJobEvents(client, jobId, options)` per the `<interfaces>` shape. Implementation:
       - Build the URL: `const url = new URL(\`\${client.baseUrl}/api/v1/jobs/\${encodeURIComponent(jobId)}/events\`); url.searchParams.set('token', options.token); if (options.lastEventId) url.searchParams.set('lastEventId', options.lastEventId);`.
       - Resolve the `EventSource` constructor: in the browser use the native `window.EventSource`; in Node dynamically import the polyfill — `const { EventSource } = typeof window === 'undefined' ? await import('eventsource') : { EventSource: window.EventSource }`. The dynamic `import('eventsource')` guarded by `typeof window === 'undefined'` keeps the polyfill OUT of the browser bundle (RESEARCH — Node-only conditional import). Since the import is async, `subscribeJobEvents` either returns a `Promise<() => void>` or accepts that the first events may be missed during the import tick — prefer returning a `Promise<() => void>` so callers `await` it before relying on the subscription.
       - `es.onmessage = (e) => options.onEvent(JSON.parse(e.data))`.
       - `es.addEventListener('replay_truncated', (e) => ...)` — surface the truncation signal: either call a dedicated `options.onReplayTruncated?` callback (preferred — add it to the options type) or deliver it through `onEvent` with a discriminable shape. Pick the dedicated callback; document it.
       - `if (options.onError) es.onerror = options.onError`.
       - Return `() => es.close()` (the unsubscribe).
       - Keep a `JobEvent` type export consistent with the server's event shape.
       - Decide on the non-streaming stub: the Phase 16 stub `getJobEvents` (returns `Promise<JobEvent[]>`) — keep it as a thin convenience wrapper OR remove it if nothing imports it. Grep the repo for `getJobEvents` usage first; if `@spatula/cli` or tests import it, keep it and add `subscribeJobEvents` alongside; if unused, replace it. Whichever path, update `packages/client/src/index.ts` exports so `subscribeJobEvents` is public.
    3. Create `packages/client/src/methods/get-job-events.test.ts` with the `<behavior>` tests. Mock `EventSource` (assign a fake to `globalThis.EventSource` or inject) so the unit test does not need a live server — assert URL construction, message parsing, the replay_truncated path, and unsubscribe.
    4. Verify the size budget: run the package's `size-limit` check (`pnpm --filter @spatula/client run size` or whatever the script is named — check `package.json` scripts). It must still pass the ≤50 KB gzipped gate. If it regresses, confirm the `eventsource` import is genuinely dynamic + Node-guarded (a static top-level `import 'eventsource'` would pull it into the bundle — that is the likely cause).

  </action>
  <verify>
    <automated>pnpm --filter @spatula/client exec vitest run src/methods/get-job-events.test.ts 2>&1 | tail -10 && pnpm --filter @spatula/client run size 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/package.json` `dependencies` contains `"eventsource": "4.1.0"`
    - `packages/client/src/methods/get-job-events.ts` exports `subscribeJobEvents` and contains `EventSource` and a `typeof window === 'undefined'`-guarded dynamic `import('eventsource')`
    - `subscribeJobEvents` sets `token` and (when provided) `lastEventId` as query params and returns an unsubscribe function
    - `packages/client/src/index.ts` exports `subscribeJobEvents`
    - `packages/client/src/methods/get-job-events.test.ts` exists and covers URL build, message parse, replay_truncated, unsubscribe
    - `pnpm --filter @spatula/client exec vitest run src/methods/get-job-events.test.ts` exits 0
    - `pnpm --filter @spatula/client run size` exits 0 (≤50 KB gzipped gate still green)
  </acceptance_criteria>
  <done>@spatula/client has a real SSE streaming method using native EventSource in the browser and the polyfill in Node, passing token + lastEventId, staying within the size budget.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright browser e2e — full OIDC + SSE reconnect chain</name>
  <files>tests/e2e/browser/vitest.config.ts, tests/e2e/browser/oidc-sse-flow.spec.ts, tests/e2e/browser/README.md</files>
  <read_first>
    - .planning/phases/17-browser-auth-sse-cors/17-05-SUMMARY.md (the examples/auth-dex/ kit — client ids, issuer, dev login, check-dex.ts)
    - .planning/phases/17-browser-auth-sse-cors/17-02-SUMMARY.md (the SSE endpoint behavior — headers, replay, resume)
    - examples/auth-dex/README.md (the JWT_ISSUER / JWT_AUDIENCE env vars the API needs to trust Dex)
    - tests/contract/helpers/server-harness.ts (how to boot the apps/api server for an e2e suite)
    - tests/e2e/ (existing e2e suite layout + vitest config style)
    - .planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md (Environment Availability — Playwright 1.58.2 in pnpm store; `playwright install chromium` required; Pattern 1 + Don't Hand-Roll — browser does PKCE natively)
  </read_first>
  <action>
    Create the `tests/e2e/browser/` Playwright suite proving ROADMAP success criterion 1 — the full browser OIDC + SSE reconnect chain.

    1. `tests/e2e/browser/vitest.config.ts` — copy the existing `tests/e2e/` vitest config style; set the `include` glob to `tests/e2e/browser/**/*.spec.ts`; give a generous `testTimeout` / `hookTimeout` (e2e + Docker boot — 120s+ per VALIDATION.md estimated runtime). If Playwright is driven via vitest, ensure `playwright` (`chromium`) is wired; alternatively use `@playwright/test` directly with its own config — pick whichever matches the existing `tests/e2e/` convention. Document the choice in the README (step 3).
    2. `tests/e2e/browser/oidc-sse-flow.spec.ts` — the e2e test. It must, in `beforeAll`:
       - Ensure Dex is up: bring up `examples/auth-dex/` (`docker compose up -d` in that dir) OR assume it is already running and gate on `examples/auth-dex/smoke/check-dex.ts` (the probe from plan 17-05). Use a `waitFor`/poll on the discovery doc.
       - Boot the apps/api server with `AUTH_STRATEGY=jwt`, `JWT_ISSUER=http://localhost:5556/dex`, `JWT_AUDIENCE=spatula-browser`, `JWT_JWKS_URL=http://localhost:5556/dex/keys`, plus a real Postgres + Redis (contract-harness style). `playwright install chromium` must have run — add a note/guard.
       - Launch a Chromium browser context.
       The test body — the full chain:
       1. **OIDC login**: navigate the browser through the Dex authorization-code + PKCE flow for the `spatula-browser` client (redirect `http://localhost:3000/callback`); log in with the dev credentials `dev@example.com` / `password`; capture the resulting ID/access JWT. The browser does PKCE natively — drive the form, do not hand-roll the code verifier (RESEARCH Don't Hand-Roll).
       2. **Create a job**: with the JWT, `POST /api/v1/jobs` to create a job to subscribe to (or seed one directly).
       3. **Get a stream token**: `POST /api/v1/ws-token` with the JWT → `{ token }`.
       4. **SSE subscribe**: open `GET /api/v1/jobs/:id/events?token=<token>` (use the `subscribeJobEvents` SDK method from Task 1, or a raw `EventSource` in the page context). Publish a few job events server-side (via `RedisEventPublisher`) and assert they arrive in order with monotonic `id:` values.
       5. **Mid-stream disconnect**: close the SSE connection after capturing the last received event id.
       6. **Reconnect with Last-Event-ID**: open a NEW SSE connection passing the captured id as `lastEventId`. Publish more events. Assert the reconnected stream resumes strictly AFTER the captured id (no duplicates, no gaps) — and that buffered events published during the disconnect window are replayed.
       - Tear down: close the browser, the API server, and (if this suite started it) the Dex compose stack.
    3. `tests/e2e/browser/README.md` — document how to run the suite: prerequisites (`playwright install chromium`, Docker for the Dex kit, Postgres + Redis), the command, and the env vars. Note that this suite is heavy (Docker + browser) and runs on `main`/tags, not every PR (per spec §6 CI topology — e2e lane).

  </action>
  <verify>
    <automated>test -f tests/e2e/browser/oidc-sse-flow.spec.ts && test -f tests/e2e/browser/vitest.config.ts && grep -q "Last-Event-ID\|lastEventId" tests/e2e/browser/oidc-sse-flow.spec.ts && grep -qi "ws-token" tests/e2e/browser/oidc-sse-flow.spec.ts && echo "BROWSER_E2E_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/browser/vitest.config.ts` and `tests/e2e/browser/oidc-sse-flow.spec.ts` exist
    - `oidc-sse-flow.spec.ts` drives all chain steps: Dex OIDC login, `POST /api/v1/ws-token`, SSE subscribe, mid-stream disconnect, reconnect with `lastEventId`, resume assertion
    - `oidc-sse-flow.spec.ts` references the Dex kit (`examples/auth-dex` / `localhost:5556`) and asserts resumed events are strictly after the captured id
    - `tests/e2e/browser/README.md` documents `playwright install chromium`, the Docker/Postgres/Redis prerequisites, and the run command
    - The suite runs to green when Dex + Postgres + Redis + chromium are available (`pnpm exec vitest run --config tests/e2e/browser/vitest.config.ts` exits 0 in that environment)
  </acceptance_criteria>
  <done>A Playwright e2e proves the full browser chain — OIDC login, stream-token, SSE subscribe, disconnect, reconnect with Last-Event-ID, resume — without manual intervention.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @spatula/client exec vitest run src/methods/get-job-events.test.ts` exits 0
- `pnpm --filter @spatula/client run size` exits 0 (≤50 KB gzipped)
- `tests/e2e/browser/oidc-sse-flow.spec.ts` runs green against a live Dex + API + Postgres + Redis
- The reconnect resumes strictly after the last-seen event id with buffered-event replay
</verification>

<success_criteria>

- ROADMAP success criterion 1: a browser smoke client performs the full chain — OIDC login via Dex → ws-token → SSE subscribe → disconnect → reconnect with Last-Event-ID → resume — without manual intervention
- @spatula/client ships a real SSE streaming method within the size budget
  </success_criteria>

<output>
After completion, create `.planning/phases/17-browser-auth-sse-cors/17-06-SUMMARY.md`
</output>

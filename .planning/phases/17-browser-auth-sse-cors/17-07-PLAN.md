---
phase: 17-browser-auth-sse-cors
plan: 07
type: execute
wave: 3
depends_on: ['01', '02', '03', '04', '05']
files_modified:
  - tests/isolation/fixtures.ts
  - tests/isolation/generator.ts
  - tests/isolation/cross-tenant.test.ts
  - tests/e2e/m2m/vitest.config.ts
  - tests/e2e/m2m/client-credentials.spec.ts
  - tests/e2e/m2m/README.md
autonomous: true
requirements: [AUTH-07, AUTH-08]
must_haves:
  truths:
    - 'tests/isolation/ seeds tenants A and B with one resource per resource type'
    - 'The isolation suite iterates every authed route in the served OpenAPI spec, and the SSE route (registered via createRoute in plan 17-02) is included in that enumeration'
    - 'A tenant-B token against a tenant-A resource path returns 403 or 404 with the standard error envelope'
    - 'No isolation-failure response leaks tenant data in error.message or error.details'
    - 'An M2M e2e proves Dex client_credentials -> JWT -> createJob -> listJobs -> getEntities'
  artifacts:
    - path: 'tests/isolation/generator.ts'
      provides: 'OpenAPI-driven route enumeration + cross-tenant assertion logic'
      contains: 'openapi'
    - path: 'tests/isolation/cross-tenant.test.ts'
      provides: 'The isolation matrix suite (AUTH-07)'
    - path: 'tests/e2e/m2m/client-credentials.spec.ts'
      provides: 'M2M OIDC client_credentials e2e (AUTH-08)'
      contains: 'client_credentials'
  key_links:
    - from: 'tests/isolation/generator.ts'
      to: 'GET /api/v1/openapi.json'
      via: 'fetch the served spec and enumerate authed routes'
      pattern: 'openapi.json'
    - from: 'tests/e2e/m2m/client-credentials.spec.ts'
      to: 'examples/auth-dex/ spatula-m2m client'
      via: 'client_credentials grant against Dex token endpoint'
      pattern: 'client_credentials'
---

<objective>
Close the audit + M2M side of Phase 17. Build `tests/isolation/` — a table-driven, OpenAPI-driven cross-tenant audit suite (AUTH-07) that seeds tenants A and B with one resource per resource type, then iterates EVERY authed route in the served `/api/v1/openapi.json` and asserts a tenant-B token against tenant A's resource path returns `403` or `404` with the standard error envelope and zero leaked tenant data. Then write the M2M e2e (`tests/e2e/m2m/`, AUTH-08) proving the OIDC `client_credentials` chain — Dex service token → `createJob` → `listJobs` → `getEntities` via `@spatula/client`.

Purpose: AUTH-07 is the "tenant A can never see tenant B" guarantee — generating the matrix from OpenAPI means new routes (the SSE + rotate routes from this very phase) are covered automatically with no manual test list to forget. AUTH-08 proves machine-to-machine auth works against a real IDP.
Output: The `tests/isolation/` suite (fixtures + generator + test) and the `tests/e2e/m2m/` suite (config + spec + README).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md
@.planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md
@.planning/phases/17-browser-auth-sse-cors/17-01-SUMMARY.md
@.planning/phases/17-browser-auth-sse-cors/17-02-SUMMARY.md
@.planning/phases/17-browser-auth-sse-cors/17-04-SUMMARY.md
@.planning/phases/17-browser-auth-sse-cors/17-05-SUMMARY.md

<interfaces>
<!-- tests/isolation/vitest.config.ts is created in plan 17-01 (Wave 1) — this plan depends_on "01" for it
     AND for the api_keys supersedes/supersededExpiresAt columns the fixture seeder touches. -->

<!-- Contract harness (tests/contract/helpers/server-harness.ts) — reuse for isolation suite -->

```typescript
export interface ContractServer {
  url: string;
  pool: Pool;
  close(): Promise<void>;
}
// boots apps/api behind node:http with real Postgres + Redis; seedTenantAndKey() helper exists in tests/contract/helpers/
```

<!-- Frozen ErrorCode values the isolation suite asserts (D-19's descriptive labels mapped to real codes by RESEARCH Pitfall 5):
       D-19 "RESOURCE_NOT_FOUND"  -> ErrorCode.RESOURCE_NOT_FOUND = 'RESOURCE.NOT_FOUND'   (added in plan 17-01, 404)
       D-19 "INSUFFICIENT_SCOPE"  -> ErrorCode.AUTH_INSUFFICIENT_SCOPE = 'AUTH.INSUFFICIENT_SCOPE'  (403)
       D-19 "TENANT_MISMATCH"     -> no code; D-18 says cross-tenant access returns 404, not a distinct code -->

<!-- Error envelope shape (frozen Phase 16): { error: { code, message, requestId, details? } } -->

<!-- D-18 status-code policy: prefer 404 (resource "doesn't exist" for this tenant) over 403;
     403 reserved for global resources where the action is scope-gated (e.g. admin-only). -->

<!-- SSE route registration (from plan 17-02): GET /api/v1/jobs/:id/events is registered via
     @hono/zod-openapi createRoute (apps/api/src/routes/job-events.ts) so it APPEARS in
     /api/v1/openapi.json under paths. The isolation generator enumerates it like any other route. -->

<!-- M2M: Dex spatula-m2m confidential client, secret dev-only-secret-m2m, token endpoint
     http://localhost:5556/dex/token, grant_type=client_credentials. Dex sets sub=client_id;
     JwtAuthProvider auto-provisions a tenant on first use via the user_tenants path. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the isolation fixtures + OpenAPI-driven generator</name>
  <files>tests/isolation/fixtures.ts, tests/isolation/generator.ts</files>
  <read_first>
    - tests/contract/helpers/server-harness.ts (the boot-the-app harness + seedTenantAndKey to extend)
    - tests/contract/helpers/fixtures.ts (existing fixture helpers)
    - tests/contract/generated.test.ts (how the contract suite enumerates routes from the served OpenAPI spec — the enumeration pattern to mirror)
    - tests/isolation/vitest.config.ts (the config scaffolded in plan 17-01)
    - apps/api/src/routes/jobs.ts, entities.ts, extractions.ts, actions.ts, exports.ts, schemas.ts (resource routes — informs what to seed)
    - .planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md (D-17, D-18, D-19 — table-driven generator, status-code policy, error-code assertions)
    - .planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md (Pitfall 5 — actual ErrorCode values; Don't Hand-Roll — OpenAPI-driven enumeration)
  </read_first>
  <action>
    Create two files: `tests/isolation/fixtures.ts` and `tests/isolation/generator.ts`.

    `tests/isolation/fixtures.ts` — seed helpers. Extend the `seedTenantAndKey` pattern from `tests/contract/helpers/`:
    - `seedTenantWithResources(pool, label): Promise<SeededTenant>` — creates a tenant, an API key (with full default scopes), AND one resource per resource type owned by that tenant. Per D-17 seed: a job, an entity, an extraction, an action, an export, a schema, an api-key, a dlq-entry, an audit-log row, and a content-store blob — as many as are reachable via authed routes. For resource types that require a parent (entities/extractions/actions/exports belong to a job), seed the parent first and nest under it.
    - The returned `SeededTenant` exposes the tenant id, the bearer token (raw API key), and a map of `{ resourceType: resourceId }` so the generator can build paths like `/api/v1/jobs/{jobId}/entities/{entityId}`.
    - Call this twice in the suite to seed tenant A and tenant B.

    `tests/isolation/generator.ts` — the OpenAPI-driven route enumeration + assertion logic (D-17, Don't Hand-Roll):
    - `enumerateAuthedRoutes(openapiSpec): RouteCase[]` — read the served `/api/v1/openapi.json` (fetched at suite boot, same source `tests/contract/` uses). For every `(path, method)` in the spec, classify it: is it an authed route (under `/api/v1/`, not `/health`, not `/api/v1/openapi.json`, not `/.well-known/*`, not `POST /api/v1/ws-token` which is tenant-scoped but issues tokens, not `POST /api/v1/tenants` which is creation-secret-gated)? Skip non-authed and non-resource-scoped routes. For the remaining routes, identify which path params map to a seeded resource id. NOTE: the SSE route `GET /api/v1/jobs/{id}/events` IS in the spec (plan 17-02 registers it via `createRoute`) — it will appear in this enumeration and must NOT be skipped.
    - `buildCrossTenantCase(route, tenantA, tenantB): TestCase` — substitute tenant A's resource ids into the route path, but use tenant B's bearer token. The expectation: the response is `403` OR `404` (D-18 — prefer 404; the test asserts whichever the route's OpenAPI `responses` declares, or accepts either if the route declares both). SPECIAL CASE for the SSE route: SSE auth is via the `?token=` query param, NOT the bearer header — so for the SSE route the generator builds the cross-tenant case using a tenant-B stream token (issued via `POST /api/v1/ws-token` with tenant B's bearer key) appended as `?token=`, against tenant A's job id. Flag this case (e.g. `authMode: 'stream-token'`) so Task 2's test runner knows to mint a stream token instead of sending a bearer header.
    - `assertIsolated(response): void` — assert (a) status is 403 or 404; (b) the body is the standard envelope `{ error: { code, message, requestId } }`; (c) `error.code` is one of `'RESOURCE.NOT_FOUND'` or `'AUTH.INSUFFICIENT_SCOPE'` (the REAL frozen values — RESEARCH Pitfall 5 maps D-19's descriptive labels to these; there is no `TENANT_MISMATCH` code, D-18's 404-preference handles that case); (d) NO tenant-A data leaks — `error.message` and `JSON.stringify(error.details ?? {})` must NOT contain tenant A's tenant id, resource ids, or any seeded resource name.
    - Export a `coverageReport()` that lists every authed route the generator found vs every route it actually asserted, so the suite can fail if a route was discovered but skipped without a documented reason — this is the "no forgotten endpoint" guarantee (D-17).

  </action>
  <verify>
    <automated>test -f tests/isolation/fixtures.ts && test -f tests/isolation/generator.ts && grep -qi "openapi" tests/isolation/generator.ts && grep -q "RESOURCE.NOT_FOUND\|AUTH.INSUFFICIENT_SCOPE" tests/isolation/generator.ts && echo "GENERATOR_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/isolation/fixtures.ts` exports a seed helper that creates a tenant + API key + one resource per resource type
    - `tests/isolation/generator.ts` enumerates routes from the served OpenAPI spec (not a hand-maintained list) and skips non-authed routes
    - `generator.ts` asserts the cross-tenant response is 403 or 404, conforms to the standard envelope, and `error.code` is `RESOURCE.NOT_FOUND` or `AUTH.INSUFFICIENT_SCOPE`
    - `generator.ts` asserts no tenant-A id/name appears in `error.message` or `error.details`
    - `generator.ts` builds the SSE-route cross-tenant case with a `?token=` stream token (not a bearer header) — it does NOT skip the SSE route
    - `generator.ts` exposes a coverage report distinguishing discovered vs asserted routes
  </acceptance_criteria>
  <done>The isolation suite has fixtures seeding two fully-resourced tenants and an OpenAPI-driven generator that builds cross-tenant test cases — including the SSE route via its stream-token auth path — with leak-proof envelope assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the cross-tenant isolation test suite</name>
  <files>tests/isolation/cross-tenant.test.ts</files>
  <read_first>
    - tests/isolation/generator.ts and tests/isolation/fixtures.ts (the modules from Task 1)
    - tests/isolation/vitest.config.ts (the config from plan 17-01)
    - tests/contract/generated.test.ts (the describe.each / it.each matrix pattern to mirror)
    - .planning/phases/17-browser-auth-sse-cors/17-02-SUMMARY.md and 17-04-SUMMARY.md (confirm the SSE route is registered via createRoute and the rotate route is registered — both must appear in the OpenAPI enumeration)
    - .planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md (D-17 — reuse the Phase 16 harness)
  </read_first>
  <action>
    Create `tests/isolation/cross-tenant.test.ts` — the matrix suite.

    In `beforeAll`: boot the apps/api server with the contract harness (real Postgres + Redis); fetch `GET /api/v1/openapi.json`; seed tenant A and tenant B via `seedTenantWithResources` (Task 1 fixtures).

    Body: call `enumerateAuthedRoutes(spec)`, then for each route case use `describe.each` + `it.each` (mirror `tests/contract/generated.test.ts`) to generate one test per `(authed route, resource)` tuple. Each test:
    - Builds the cross-tenant request (tenant A's resource path + tenant B's credentials) via `buildCrossTenantCase`.
    - Issues the HTTP request against the harness URL. For routes flagged `authMode: 'stream-token'` (the SSE route), first mint a tenant-B stream token via `POST /api/v1/ws-token` with tenant B's bearer key, then issue `GET <path>?token=<tenantB-stream-token>`. For all other routes, send tenant B's bearer header.
    - Runs `assertIsolated(response)`.

    Additional assertions:
    - A positive control: tenant A's token against tenant A's resource succeeds (proves the resources were actually seeded and reachable — otherwise a 404 could be a false pass).
    - The SSE route (`GET /api/v1/jobs/{id}/events`) and the rotate route (`POST /api/v1/api-keys/{id}/rotate`) — newly added this phase — MUST appear in the enumerated route set. Add an explicit assertion that both paths are present in the `paths` of the fetched `/api/v1/openapi.json` (this is why this plan `depends_on` 17-02 and 17-04 — 17-02 registers the SSE route via `createRoute` so it lands in the spec, 17-04 registers the rotate route the same way). For the SSE route specifically, the cross-tenant test issues `GET /api/v1/jobs/{tenantA-jobId}/events?token=<tenantB-stream-token>` (SSE auth is via `?token=`, not the bearer header), expecting 403/404 with the standard envelope.
    - At the end, assert the `coverageReport()` shows zero discovered-but-unasserted routes (or only routes on a small documented skip list with a recorded reason).

  </action>
  <verify>
    <automated>pnpm exec vitest run --config tests/isolation/vitest.config.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/isolation/cross-tenant.test.ts` exists and uses `describe.each`/`it.each` over the OpenAPI-enumerated routes
    - The suite includes a positive control (tenant A → tenant A resource succeeds)
    - The suite explicitly asserts the SSE route path `/api/v1/jobs/{id}/events` and the rotate route path `/api/v1/api-keys/{id}/rotate` are present in the fetched `/api/v1/openapi.json` `paths`
    - The SSE cross-tenant case uses a tenant-B `?token=` stream token (not a bearer header) and asserts 403/404
    - The suite asserts the coverage report has no unexplained discovered-but-unasserted routes
    - `pnpm exec vitest run --config tests/isolation/vitest.config.ts` exits 0
  </acceptance_criteria>
  <done>tests/isolation/ proves tenant A cannot read tenant B via any authed route — every route enumerated from OpenAPI (the SSE route included, via its createRoute registration), every cross-tenant response 403/404 with a leak-free standard envelope.</done>
</task>

<task type="auto">
  <name>Task 3: M2M OIDC client_credentials e2e against Dex</name>
  <files>tests/e2e/m2m/vitest.config.ts, tests/e2e/m2m/client-credentials.spec.ts, tests/e2e/m2m/README.md</files>
  <read_first>
    - .planning/phases/17-browser-auth-sse-cors/17-05-SUMMARY.md (the Dex kit — spatula-m2m client id + secret, token endpoint, check-dex.ts, and the smoke/m2m-flow.ts reference script this e2e extends)
    - examples/auth-dex/config/dex.yaml (the spatula-m2m static client definition)
    - examples/auth-dex/smoke/m2m-flow.ts (the D-11 client_credentials reference script — this e2e extends it into the full SDK chain)
    - apps/api/src/auth/jwt-provider.ts (JwtAuthProvider — iss/aud validation, user_tenants claims-mapping; M2M sub = client_id)
    - tests/e2e/m2m/.gitkeep (scaffolded dir from plan 17-01) and tests/e2e/ (existing e2e vitest config style)
    - packages/client/src/client.ts (SpatulaClient — createJob/listJobs/getEntities used by the e2e)
    - .planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md (D-20 — the full M2M chain)
    - .planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md (Code Examples — Dex JWT claims shape; the M2M sub=client_id + auto-tenant note)
  </read_first>
  <action>
    Create the `tests/e2e/m2m/` suite proving AUTH-08 / D-20 — the OIDC `client_credentials` machine-to-machine chain. This suite extends `examples/auth-dex/smoke/m2m-flow.ts` (the D-11 reference script from plan 17-05) — that script proves the token-grant half; this suite proves the full SDK chain on top of it.

    1. `tests/e2e/m2m/vitest.config.ts` — copy the `tests/e2e/` vitest config style; `include` glob `tests/e2e/m2m/**/*.spec.ts`; generous timeouts (Docker + e2e).
    2. `tests/e2e/m2m/client-credentials.spec.ts` — in `beforeAll`: ensure Dex is up (gate on `examples/auth-dex/smoke/check-dex.ts` or bring up the compose stack); boot apps/api with `AUTH_STRATEGY=jwt`, `JWT_ISSUER=http://localhost:5556/dex`, `JWT_AUDIENCE=spatula-m2m`, `JWT_JWKS_URL=http://localhost:5556/dex/keys`, plus real Postgres + Redis.
       The test body — the D-20 chain:
       1. **Get a service token**: POST to the Dex token endpoint `http://localhost:5556/dex/token` with `grant_type=client_credentials`, `client_id=spatula-m2m`, `client_secret=dev-only-secret-m2m` (the committed dev secret), and the appropriate `scope`. Capture the JWT. Confirm the JWT `sub` equals the client id `spatula-m2m` and `aud` includes `spatula-m2m` (RESEARCH — Dex sets `sub` = client_id for client_credentials). Reuse the token-grant logic from `examples/auth-dex/smoke/m2m-flow.ts` (import or mirror it — do not re-derive).
       2. **Create a job**: instantiate `@spatula/client`'s `SpatulaClient` with the service JWT as the bearer token; call `createJob(...)`. The first call exercises the `JwtAuthProvider` `user_tenants` auto-provision path (a tenant is created for the M2M `sub` on first use) — assert the job is created (201/200).
       3. **List jobs**: call `listJobs()`; assert the just-created job appears.
       4. **Get entities**: call `getEntities(jobId)` (it may be empty if the job hasn't crawled — assert the call succeeds with a 200 and a well-formed cursor envelope, not that entities exist).
       - Tear down: close the API server; close the Dex stack if this suite started it.
    3. `tests/e2e/m2m/README.md` — document prerequisites (Docker for the Dex kit, Postgres, Redis), the run command, the env vars, and that the suite runs on `main`/tags (e2e CI lane), not every PR.

  </action>
  <verify>
    <automated>test -f tests/e2e/m2m/client-credentials.spec.ts && test -f tests/e2e/m2m/vitest.config.ts && grep -q "client_credentials" tests/e2e/m2m/client-credentials.spec.ts && grep -q "spatula-m2m" tests/e2e/m2m/client-credentials.spec.ts && echo "M2M_E2E_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/m2m/vitest.config.ts` and `tests/e2e/m2m/client-credentials.spec.ts` exist
    - The spec POSTs `grant_type=client_credentials` to the Dex token endpoint with `client_id=spatula-m2m` and the committed dev secret
    - The spec drives the full chain: service token → `createJob` → `listJobs` → `getEntities` via `@spatula/client`
    - `tests/e2e/m2m/README.md` documents the prerequisites and run command
    - The suite runs to green when Dex + Postgres + Redis are available (`pnpm exec vitest run --config tests/e2e/m2m/vitest.config.ts` exits 0 in that environment)
  </acceptance_criteria>
  <done>An e2e proves the OIDC client_credentials M2M chain end to end — a Dex service token drives createJob, listJobs, and getEntities through the SDK, building on the plan 17-05 m2m-flow reference script.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run --config tests/isolation/vitest.config.ts` exits 0
- `pnpm exec vitest run --config tests/e2e/m2m/vitest.config.ts` exits 0 against a live Dex + Postgres + Redis
- Every authed route enumerated from OpenAPI is asserted; cross-tenant access yields 403/404 with no leaked data
- The SSE + rotate routes from this phase are present in the OpenAPI spec and covered by the isolation matrix
</verification>

<success_criteria>

- AUTH-07: tests/isolation/ proves tenant A cannot read tenant B via any authed route, every assertion 403/404 with the standard envelope and no leaked tenant data
- AUTH-08: M2M OIDC client_credentials validated e2e against Dex — service token creates a job and lists entities via the SDK
  </success_criteria>

<output>
After completion, create `.planning/phases/17-browser-auth-sse-cors/17-07-SUMMARY.md`
</output>
</content>
</invoke>

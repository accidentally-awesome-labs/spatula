---
phase: 17-browser-auth-sse-cors
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/auth-dex/docker-compose.yml
  - examples/auth-dex/config/dex.yaml
  - examples/auth-dex/README.md
  - examples/auth-dex/.gitignore
  - examples/auth-dex/smoke/check-dex.ts
autonomous: false
requirements: [AUTH-04]
must_haves:
  truths:
    - "docker compose up in examples/auth-dex/ produces a working Dex OIDC IDP"
    - "Dex exposes a browser PKCE client and an M2M client_credentials client"
    - "The Dex discovery doc is reachable at the issuer's /.well-known/openid-configuration"
    - "examples/auth-dex/README.md is a zero-config walkthrough"
  artifacts:
    - path: "examples/auth-dex/docker-compose.yml"
      provides: "Dex container definition with SQLite storage volume"
      contains: "dex"
    - path: "examples/auth-dex/config/dex.yaml"
      provides: "Dex static config — issuer, sqlite3 storage, two static clients, one connector"
      contains: "staticClients"
    - path: "examples/auth-dex/README.md"
      provides: "Zero-config walkthrough for booting the example IDP"
  key_links:
    - from: "examples/auth-dex/docker-compose.yml"
      to: "examples/auth-dex/config/dex.yaml"
      via: "config file mounted into the dex container"
      pattern: "config"
---

<objective>
Ship `examples/auth-dex/` — a self-contained, zero-config local OIDC recipe (AUTH-04). `docker compose up` in this directory must produce a working Dex IDP that a browser smoke client and an M2M client can target without environment surgery. The kit ships a `docker-compose.yml` (Dex with a SQLite-backed mounted volume — boots in <10s on a clean Mac, the AUTH-04 acceptance criterion), a static `config/dex.yaml` with one connector and two static clients (a public PKCE client for the browser code flow and a confidential client for `client_credentials`), a `README.md` walkthrough, and a small `check-dex.ts` health probe.

Purpose: AUTH-04, AUTH-08, and the Wave-2 browser/M2M e2e tests all need a running IDP. This is the foundation fixture for those suites — it must exist and boot before plans 17-06 and 17-07 can run their e2e tests.
Output: The `examples/auth-dex/` directory with compose, Dex config, README, gitignore, and a discovery-doc health check. This plan has ONE checkpoint — the operator verifies `docker compose up` produces a healthy IDP in <10s (wall-clock perf is not CI-reproducible per the VALIDATION.md manual-verification table).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md
@.planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md

<interfaces>
<!-- Dex minimal config (from RESEARCH Code Examples — verified against dexidp/dex v2.x) -->
```yaml
issuer: http://localhost:5556/dex
storage: { type: sqlite3, config: { file: /data/dex.db } }
web: { http: 0.0.0.0:5556 }
oauth2: { skipApprovalScreen: true, responseTypes: [code] }
connectors: [ { type: mockCallback, id: mock, name: Mock } ]
staticClients:
  - { id: spatula-browser, name: Spatula Browser, redirectURIs: [http://localhost:3000/callback], public: true }
  - { id: spatula-m2m, name: Spatula M2M, secret: dev-only-secret-m2m, redirectURIs: [] }
enablePasswordDB: true
staticPasswords: [ { email: dev@example.com, hash: "<bcrypt>", username: devuser, userID: "00000000-0000-0000-0000-000000000001" } ]
```
<!-- Dex v2.45.1 is the verified-latest release tag (RESEARCH Environment Availability). -->
<!-- M2M client_credentials: Dex sets `sub` = the client_id. The JwtAuthProvider auto-provisions a tenant on first use. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Dex docker-compose + config + gitignore</name>
  <files>examples/auth-dex/docker-compose.yml, examples/auth-dex/config/dex.yaml, examples/auth-dex/.gitignore</files>
  <read_first>
    - .planning/phases/17-browser-auth-sse-cors/17-RESEARCH.md (Code Examples — Dex minimal config; Environment Availability — Dex v2.45.1, Docker 29.3.0, Compose v5.1.0 confirmed available)
    - .planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md (D-11, D-12, D-13 — kit structure, SQLite storage, dev-only-secret banner)
    - examples/quickstart/ (existing example directory layout — match the house style of the examples/ tree)
  </read_first>
  <action>
    Create the `examples/auth-dex/` kit per D-11 / D-12 / D-13.

    `examples/auth-dex/docker-compose.yml`:
    - One service `dex` using the official image pinned to a concrete version: `ghcr.io/dexidp/dex:v2.45.1` (RESEARCH confirms v2.45.1 is latest — pin it, do not use `:latest`).
    - Port mapping `5556:5556`.
    - Mount the config: `./config/dex.yaml:/etc/dex/config.docker.yaml:ro`.
    - Mount a named volume or bind mount for SQLite state so the DB file at `/data/dex.db` survives restarts: a bind mount `./data:/data` is simplest for an example (D-12 — mounted volume).
    - Command/args: run dex with the mounted config — `command: ["dex", "serve", "/etc/dex/config.docker.yaml"]` (verify the entrypoint expectation against the v2.45.1 image; if the image entrypoint is already `dex`, the command is `["serve", "/etc/dex/config.docker.yaml"]`).
    - Add a `healthcheck` that curls the discovery doc `http://localhost:5556/dex/.well-known/openid-configuration` so `docker compose up` reports a healthy state and downstream e2e suites can wait on it.

    `examples/auth-dex/config/dex.yaml` — static config per the `<interfaces>` block:
    - `issuer: http://localhost:5556/dex`.
    - `storage: { type: sqlite3, config: { file: /data/dex.db } }` (D-12 — SQLite, not Postgres; boots fast).
    - `web: { http: 0.0.0.0:5556 }`.
    - `oauth2: { skipApprovalScreen: true, responseTypes: [code] }`.
    - `connectors`: one mock/local connector so a browser can log in without an upstream IDP. Use `enablePasswordDB: true` + a `staticPasswords` entry (`email: dev@example.com`, a real bcrypt hash of the password `password`, `username: devuser`, a fixed `userID`). Generate the bcrypt hash with cost 10 — compute the actual hash value and embed it (do not leave a `$2a$10$...` placeholder).
    - `staticClients`: TWO clients (D-11) —
      - `spatula-browser`: `public: true` (PKCE, no secret), `redirectURIs: [http://localhost:3000/callback]`.
      - `spatula-m2m`: confidential, `secret: dev-only-secret-m2m`, `redirectURIs: []` (client_credentials needs no redirect).
    - D-13: put a prominent banner comment at the TOP of `dex.yaml`: `# DO NOT USE IN PRODUCTION — static dev-only credentials committed for local examples only`. The `spatula-m2m` secret value is intentionally committed as `dev-only-secret-m2m`.

    `examples/auth-dex/.gitignore`: ignore `data/` (the SQLite DB file and any dex runtime state should not be committed).
  </action>
  <verify>
    <automated>test -f examples/auth-dex/docker-compose.yml && test -f examples/auth-dex/config/dex.yaml && grep -q "v2.45.1" examples/auth-dex/docker-compose.yml && grep -q "staticClients" examples/auth-dex/config/dex.yaml && grep -q "spatula-m2m" examples/auth-dex/config/dex.yaml && grep -q "DO NOT USE IN PRODUCTION" examples/auth-dex/config/dex.yaml && echo "DEX_KIT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/auth-dex/docker-compose.yml` exists, pins the dex image to `v2.45.1`, maps port `5556`, mounts `config/dex.yaml`, and defines a healthcheck
    - `examples/auth-dex/config/dex.yaml` exists with `issuer`, `storage.type: sqlite3`, two `staticClients` (`spatula-browser` public + `spatula-m2m` confidential), and a connector with a real bcrypt password hash (not a `$2a$10$...` placeholder)
    - `examples/auth-dex/config/dex.yaml` has a `DO NOT USE IN PRODUCTION` banner comment
    - `examples/auth-dex/.gitignore` ignores `data/`
  </acceptance_criteria>
  <done>The Dex compose + config + gitignore exist; the IDP definition uses SQLite storage, a pinned image, two static clients, and committed dev-only credentials with a clear banner.</done>
</task>

<task type="auto">
  <name>Task 2: Write the README walkthrough and a discovery-doc health probe</name>
  <files>examples/auth-dex/README.md, examples/auth-dex/smoke/check-dex.ts</files>
  <read_first>
    - examples/auth-dex/docker-compose.yml (the kit from Task 1 — the README documents this)
    - examples/auth-dex/config/dex.yaml (client ids, secret, issuer, redirect URI — the README references these)
    - examples/quickstart/ (README style of the existing examples)
    - .planning/phases/17-browser-auth-sse-cors/17-CONTEXT.md (D-11 — README is a zero-config walkthrough)
  </read_first>
  <action>
    Two artifacts: the README and a health probe script.

    `examples/auth-dex/README.md` — a zero-config walkthrough (D-11). Sections:
    - **What this is** — a one-paragraph statement: a local Dex OIDC IDP for developing/testing Spatula's browser-OIDC and M2M auth flows. Not for production.
    - **Prerequisites** — Docker + Docker Compose only. No env vars, no accounts.
    - **Boot it** — `cd examples/auth-dex && docker compose up`. State the expected outcome: Dex healthy at `http://localhost:5556/dex`, discovery doc at `http://localhost:5556/dex/.well-known/openid-configuration`, ready in <10s on a clean machine.
    - **Clients** — a table: `spatula-browser` (public PKCE client, redirect `http://localhost:3000/callback`, used by the browser smoke test) and `spatula-m2m` (confidential `client_credentials` client, secret `dev-only-secret-m2m`, used by the M2M e2e test).
    - **Dev login** — the static credentials: `dev@example.com` / `password`.
    - **Pointing Spatula at it** — the env vars the API needs to trust this IDP: `AUTH_STRATEGY=jwt`, `JWT_ISSUER=http://localhost:5556/dex`, `JWT_AUDIENCE=spatula-browser` (and how the M2M flow uses `aud=spatula-m2m`), `JWT_JWKS_URL=http://localhost:5556/dex/keys`.
    - **Security banner** — repeat D-13: all credentials here are committed dev-only secrets; never reuse them.
    - **Used by** — note that `tests/e2e/browser/` (plan 17-06) and `tests/e2e/m2m/` (plan 17-07) target this kit.

    `examples/auth-dex/smoke/check-dex.ts` — a small standalone Node script (no test framework) that the README and the e2e suites can call to confirm Dex is up: it fetches `http://localhost:5556/dex/.well-known/openid-configuration`, asserts a 200 with an `issuer` field equal to `http://localhost:5556/dex`, prints `dex-ok` and exits 0; on failure prints the error and exits 1. Keep it dependency-free (use global `fetch`). This is the runnable form of the AUTH-04 health check that downstream e2e suites can `await` on before driving flows.
  </action>
  <verify>
    <automated>test -f examples/auth-dex/README.md && test -f examples/auth-dex/smoke/check-dex.ts && grep -q "docker compose up" examples/auth-dex/README.md && grep -q "openid-configuration" examples/auth-dex/smoke/check-dex.ts && echo "README_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/auth-dex/README.md` exists and contains `docker compose up`, both client ids (`spatula-browser`, `spatula-m2m`), the dev login `dev@example.com`, and the `JWT_ISSUER` env-var guidance
    - `examples/auth-dex/smoke/check-dex.ts` exists, fetches the `/.well-known/openid-configuration` discovery doc, and exits 0 on success / 1 on failure
  </acceptance_criteria>
  <done>The kit has a zero-config README walkthrough and a dependency-free Dex health probe that downstream e2e suites can reuse.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify docker compose up boots Dex in under 10 seconds</name>
  <files>examples/auth-dex/docker-compose.yml, examples/auth-dex/config/dex.yaml, examples/auth-dex/smoke/check-dex.ts</files>
  <read_first>
    - examples/auth-dex/README.md (the walkthrough being verified)
    - .planning/phases/17-browser-auth-sse-cors/17-VALIDATION.md (manual-verification table — confirms this is operator-verified, not CI)
  </read_first>
  <what-built>
    A self-contained `examples/auth-dex/` OIDC kit: `docker-compose.yml` (Dex v2.45.1, SQLite storage), `config/dex.yaml` (issuer + two static clients + dev login), `README.md` walkthrough, and a `smoke/check-dex.ts` discovery-doc probe.
  </what-built>
  <action>
    This is a checkpoint task — Claude has already authored the kit in Tasks 1 and 2. There is no further automation: the AUTH-04 acceptance criterion "boots in <10s on a clean Mac" is wall-clock perf on operator hardware and is not CI-reproducible (VALIDATION.md manual-verification table). Present the kit to the operator and pause for the manual verification described in `<how-to-verify>`. Resume on the `<resume-signal>`.
  </action>
  <how-to-verify>
    The AUTH-04 acceptance criterion "boots in <10s on a clean Mac" is wall-clock perf on operator hardware — not CI-reproducible (VALIDATION.md manual-verification table). Verify manually:
    1. From the repo root: `cd examples/auth-dex && docker compose up -d`.
    2. Time how long until the `dex` container reports `healthy`: `docker compose ps` should show `healthy` within ~10 seconds of the image being present (image-pull time on a first run is excluded — pull once beforehand with `docker compose pull`).
    3. Run the health probe: `node smoke/check-dex.ts` (or `npx tsx smoke/check-dex.ts`) — it should print `dex-ok` and exit 0.
    4. Open `http://localhost:5556/dex/.well-known/openid-configuration` in a browser — confirm it returns JSON with `"issuer": "http://localhost:5556/dex"` and an `authorization_endpoint`.
    5. Tear down: `docker compose down`.
  </how-to-verify>
  <verify>
    <automated>cd examples/auth-dex && docker compose up -d && node smoke/check-dex.ts && docker compose down</automated>
  </verify>
  <acceptance_criteria>
    - `cd examples/auth-dex && docker compose up -d` brings the `dex` container to a `healthy` state
    - `node examples/auth-dex/smoke/check-dex.ts` prints `dex-ok` and exits 0
    - `http://localhost:5556/dex/.well-known/openid-configuration` returns JSON with `"issuer": "http://localhost:5556/dex"`
  </acceptance_criteria>
  <resume-signal>Type "approved" if Dex boots healthy and the discovery doc resolves, or describe what failed (boot timeout, config error, port conflict).</resume-signal>
  <done>The operator confirms `docker compose up` produces a healthy Dex IDP and the OIDC discovery doc resolves.</done>
</task>

</tasks>

<verification>
- `examples/auth-dex/` contains docker-compose.yml, config/dex.yaml, README.md, .gitignore, smoke/check-dex.ts
- `docker compose up` produces a healthy Dex IDP (operator-verified)
- The discovery doc resolves at `http://localhost:5556/dex/.well-known/openid-configuration`
</verification>

<success_criteria>
- AUTH-04: examples/auth-dex/ boots with `docker compose up` and produces a working Dex IDP that browser + M2M smoke clients can target without environment surgery
</success_criteria>

<output>
After completion, create `.planning/phases/17-browser-auth-sse-cors/17-05-SUMMARY.md`
</output>

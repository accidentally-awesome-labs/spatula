---
phase: 19.1-hosted-execution-path
plan: 04
type: execute
wave: 4
depends_on:
  - "19.1-01"
  - "19.1-02"
  - "19.1-03"
files_modified:
  - docs/runbooks/hardware-sizing.md
  - docs/runbooks/render-deploy.md
files_created:
  - packages/queue/src/worker-entrypoint.test.ts
autonomous: false
requirements:
  - EXEC-04
  - EXEC-05
  - EXEC-06
must_haves:
  truths:
    - "startWorker() builds non-null deps (proven by a unit test injecting _opts.deps and one asserting buildWorkerDeps fail-loud)"
    - "A crawl submitted via the API is processed to completed with pages > 0 and extractions; GET /api/v1/jobs/:id exposes stats.pagesCompleted; the CLI local crawl still works (no regression)"
    - "GET /api/v1/usage returns non-zero totalCostUsd with correct per-job byJob after a real crawl (local docker smoke)"
    - "pnpm sizing:baseline yields a 3-tier table with non-zero, tier-distinct cost/page (EXEC-06 harness green)"
    - "DEPLOY-02 re-verified LIVE on Render — a real crawl completes (pages + non-zero /usage cost), clearing the 19-05 caveat (EXEC-05)"
  artifacts:
    - path: "packages/queue/src/worker-entrypoint.test.ts"
      provides: "unit tests: deps non-null via injected _opts.deps; ALS attribution; per-job model derivation"
      contains: "startWorker"
    - path: "docs/runbooks/hardware-sizing.md"
      provides: "the local smoke + sizing re-run procedure with the verified non-zero/tier-distinct result recorded"
      contains: "sizing:baseline"
    - path: "docs/runbooks/render-deploy.md"
      provides: "the EXEC-05 live re-verify steps (SPATULA_CRAWLER=firecrawl + FIRECRAWL_API_KEY note) clearing the 19-05 caveat"
      contains: "SPATULA_CRAWLER"
  key_links:
    - from: "scripts/sizing-baseline.ts"
      to: "the running local stack (API :3100 + embedded worker)"
      via: "SIZING_PAGES=3 SPATULA_API_URL=http://localhost:3100 pnpm sizing:baseline → 3-row table, non-zero tier-distinct cost/page"
      pattern: "cost/page"
    - from: "Render embedded worker"
      to: "GET /api/v1/usage"
      via: "live crawl → byJob shows non-zero cost (EXEC-05)"
      pattern: "totalCostUsd|byJob"
---

<objective>
Verify the hosted execution path end-to-end and clear the two blocked requirements (DEPLOY-02 live re-verify, DEPLOY-09 sizing harness). This plan adds unit tests proving the three gaps are closed structurally, runs the LOCAL docker+embedded smoke that previously DLQ'd with `WorkerDeps not initialized` (now must produce a completed job + non-zero, tier-distinct cost/page), confirms no regressions (full build/lint/unit + CLI local crawl), and ends with a human-action checkpoint for the LIVE Render re-verify (a real deploy + crawler key cannot be fully automated).

Purpose: "Run 19-05 until green." This is the goal-backward proof that EXEC-01..03 actually work in production, and the gate that clears the 19-05 caveat + unblocks DEPLOY-09.
Output: a worker-entrypoint integration test, recorded sizing/smoke results in the runbooks, and a verified-live Render crawl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-hosted-execution-path/19.1-CONTEXT.md
@.planning/phases/19.1-hosted-execution-path/19.1-01-SUMMARY.md
@.planning/phases/19.1-hosted-execution-path/19.1-02-SUMMARY.md
@.planning/phases/19.1-hosted-execution-path/19.1-03-SUMMARY.md

<interfaces>
<!-- Verification assets + commands. Extracted from CONTEXT.md verification section + scripts/sizing-baseline.ts. -->

Test seam: startWorker(_opts?: { deps?: WorkerDeps }) — pass a stub WorkerDeps to avoid booting BullMQ/DB.
  (NOTE: startWorker still creates Redis/queues/workers; for a pure unit test of "deps non-null" prefer testing
   buildWorkerDeps fail-loud/non-null (Plan 01 already covers this) PLUS a focused test that startWorker honors
   _opts.deps — mock BullMQ Worker + createQueues + createDatabasePool + ioredis so no real connections open.)

Existing pattern to mirror: apps/api/src/embedded-worker.test.ts (env save/restore, vi.mock, injected factory).

LOCAL smoke (CONTEXT verification section — exact commands):
  - Stack up (ports 5433/6380 avoid host 5432/6379):
      SPATULA_PG_PORT=5433 SPATULA_REDIS_PORT=6380 docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
    (migrate runs automatically)
  - Host embedded API on :3100 with local Playwright:
      set -a; . ./.env; set +a; export SPATULA_EMBEDDED_WORKER=1 PORT=3100 NODE_ENV=production \
        DATABASE_URL=postgresql://spatula:spatula@localhost:5433/spatula REDIS_URL=redis://localhost:6380; \
        node apps/api/dist/main.js
    (requires OPENROUTER_API_KEY in ./.env; SPATULA_CRAWLER unset → playwright on the host)
  - Smoke:
      SIZING_PAGES=3 SIZING_MAX_DEPTH=5 SPATULA_API_URL=http://localhost:3100 pnpm sizing:baseline
    Expect: 3-row table, non-zero + tier-distinct cost/page, completed jobs with pages>0.

sizing:baseline (scripts/sizing-baseline.ts): sets SPATULA_LIVE_LLM gate; submits 1 job/tier; reads
  pages from GET /api/v1/jobs/:id (stats.pagesCompleted) + cost from GET /api/v1/usage (byJob).

LIVE Render (EXEC-05): srv-d8lh2q6rnols73dedvog, https://spatula-api.onrender.com (paid mirror render-paid-demo).
  Distroless embedded worker has NO Playwright → MUST set SPATULA_CRAWLER=firecrawl + FIRECRAWL_API_KEY on the
  service, then Blueprint Sync (render.yaml edits need a dashboard sync; CLI `blueprints` only validates).
</interfaces>

@scripts/sizing-baseline.ts
@apps/api/src/embedded-worker.test.ts
@docs/runbooks/hardware-sizing.md
@docs/runbooks/render-deploy.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: worker-entrypoint integration unit tests (deps non-null + ALS + per-job model)</name>
  <read_first>
    - apps/api/src/embedded-worker.test.ts (env save/restore + vi.mock + injected-factory pattern)
    - packages/queue/src/worker-entrypoint.ts (final state after Plans 01–03)
    - packages/queue/src/usage-context.ts + als-usage-recorder.ts + derive-job-deps.ts
  </read_first>
  <behavior>
    - Test: startWorker({ deps: stubDeps }) honors the injected deps — no buildWorkerDeps call, no throw (mock BullMQ Worker, createQueues, createDatabasePool, ioredis Redis so nothing real connects). Assert it returns a handle with shutdown().
    - Test (ALS attribution): inside usageContext.run({ tenantId:'T', jobId:'J' }, ...), an AlsUsageRecorder built with a mock repo records insert with tenantId 'T' + jobId 'J' (re-uses Plan 02 modules; this is a focused integration assertion that the wired recorder + context agree).
    - Test (per-job model): deriveJobDeps over a stub base + stub llmClient with { primaryModel: 'tier-fast' } produces an extractor/linkEvaluator bound to 'tier-fast'; with { primaryModel: 'tier-smart' } → 'tier-smart' — proving two jobs get distinct models (the EXEC-02/EXEC-06 race-safe-per-job invariant at unit level).
  </behavior>
  <action>
    Create `packages/queue/src/worker-entrypoint.test.ts`. Mock the heavy deps so no real I/O:
      vi.mock('bullmq', () => ({ Worker: vi.fn().mockImplementation(() => ({ on: vi.fn(), close: vi.fn().mockResolvedValue(undefined) })), Queue: vi.fn() }));
      vi.mock('ioredis', () => ({ default: vi.fn().mockImplementation(() => ({ quit: vi.fn().mockResolvedValue(undefined) })) }));
      vi.mock('@spatula/db', async (orig) => ({ ...(await orig()), createDatabasePool: vi.fn(() => ({ db: {}, pool: { end: vi.fn().mockResolvedValue(undefined) } })) }));
    Then build a `stubDeps` (a WorkerDeps-shaped object via `as any` with a `crawler.close` stub + a `queues.closeAll` stub) and call `await startWorker({ deps: stubDeps })`; assert the returned handle.shutdown is a function and resolves.
    Add the ALS + per-job assertions importing from './usage-context.js', './als-usage-recorder.js', './derive-job-deps.js' (build a stub base WorkerDeps + stub llmClient). For per-job model, assert resolveModel of the config the components were built with (or expose the model via the linkEvaluator if accessible) — simplest reliable assertion: `deriveJobDeps(base, client, { primaryModel: 'tier-fast' }, 'j').extractor !== deriveJobDeps(base, client, { primaryModel: 'tier-smart' }, 'j').extractor` AND a spy on a stub client's complete showing the right request.model when feasible; if not feasible, assert via a tiny exported getter or via resolveModel directly on the configs.
    Keep the test deterministic and fast (<5s). Restore env in afterEach like embedded-worker.test.ts.
  </action>
  <verify>
    <automated>cd /Users/salar/Projects/spatula && pnpm --filter @spatula/queue test -- worker-entrypoint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "startWorker({ deps:" packages/queue/src/worker-entrypoint.test.ts` (exit 0 — injection seam tested)
    - `grep -q "usageContext.run" packages/queue/src/worker-entrypoint.test.ts` (exit 0 — ALS attribution tested)
    - `grep -q "deriveJobDeps" packages/queue/src/worker-entrypoint.test.ts` (exit 0 — per-job model tested)
    - `pnpm --filter @spatula/queue test -- worker-entrypoint` exits 0
  </acceptance_criteria>
  <done>Unit tests prove startWorker honors injected deps (non-null), ALS attributes usage to the active job, and per-job derivation yields distinct models per job; all green and fast.</done>
</task>

<task type="auto">
  <name>Task 2: No-regression gate + LOCAL docker+embedded smoke (EXEC-04, EXEC-06)</name>
  <read_first>
    - scripts/sizing-baseline.ts (the harness invoked by the smoke)
    - docker-compose.yml + docker-compose.prod.yml (the local stack; configurable ports already added in groundwork)
    - .planning/phases/19.1-hosted-execution-path/19.1-CONTEXT.md verification section (exact commands + expected output)
    - docs/runbooks/hardware-sizing.md (where to record the smoke result)
  </read_first>
  <action>
    1. No-regression gate (must pass before the smoke):
       - `pnpm build` exits 0 (full workspace).
       - `pnpm lint` exits 0 (0 errors).
       - `pnpm --filter @spatula/queue test` + `pnpm --filter @spatula/core test` + `pnpm --filter @spatula/api test` + `pnpm --filter @spatula/cli test` green (queue/core/api/cli suites — the CLI suite covers the run.ts → LocalPipelineRunner local-crawl path; EXEC-04 no-regression).

    2. LOCAL docker+embedded smoke (the smoke that previously DLQ'd). Requires OPENROUTER_API_KEY in ./.env. Run EXACTLY (CONTEXT verification section):
       - Bring up the stack on non-colliding ports:
         `SPATULA_PG_PORT=5433 SPATULA_REDIS_PORT=6380 docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d`
       - Start a host embedded API on :3100 (local Playwright crawler) in the background:
         `set -a; . ./.env; set +a; SPATULA_EMBEDDED_WORKER=1 PORT=3100 NODE_ENV=production DATABASE_URL=postgresql://spatula:spatula@localhost:5433/spatula REDIS_URL=redis://localhost:6380 node apps/api/dist/main.js &`
       - Wait for /health, then run the 3-page-per-tier smoke:
         `SIZING_PAGES=3 SIZING_MAX_DEPTH=5 SPATULA_API_URL=http://localhost:3100 pnpm sizing:baseline`
       - EXPECT: a 3-row Markdown table; each tier's job `completed` with pages>0; cost/page NON-ZERO and TIER-DISTINCT (fast/primary/smart differ). Capture stdout + scripts/.sizing-results.json.

    3. Record the smoke outcome in `docs/runbooks/hardware-sizing.md` — add/update a "Local smoke verification (Phase 19.1)" note with the date, the SIZING_PAGES=3 result table (or a paste of the 3 rows), and the exact re-run command. This documents that the harness now produces non-zero tier-distinct cost/page (EXEC-06 harness green). Do NOT claim the full CX32 1k-per-tier table — that remains DEPLOY-09's live-run checkpoint.

    4. Tear down: `docker compose -f docker-compose.yml -f docker-compose.prod.yml down` and kill the background API.

    If OPENROUTER_API_KEY is absent or Docker is unavailable in this environment, STOP and surface a checkpoint:human-action asking the user to run the smoke block above and paste the 3-row table — do NOT fabricate results. (The build/lint/unit gate in step 1 is still mandatory and fully automatable.)
  </action>
  <verify>
    <automated>cd /Users/salar/Projects/spatula && pnpm build && pnpm lint && pnpm --filter @spatula/queue test && pnpm --filter @spatula/core test && pnpm --filter @spatula/cli test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; `pnpm lint` exits 0
    - queue/core/cli (and api) test suites exit 0 (CLI local-crawl path green → EXEC-04 no-regression)
    - After the smoke: `node -e "const r=require('./scripts/.sizing-results.json'); const c=r.results.map(x=>x.costPerPageUsd); if(!(r.results.length===3 && c.every(v=>v>0) && new Set(c).size>1)) process.exit(1)"` exits 0 (3 tiers, all cost/page > 0, at least 2 distinct) — OR the human-action checkpoint records the pasted 3-row table with the same property.
    - `grep -q "sizing:baseline" docs/runbooks/hardware-sizing.md` (exit 0 — re-run command documented)
    - `grep -qi "Phase 19.1\|local smoke" docs/runbooks/hardware-sizing.md` (exit 0 — smoke result recorded)
  </acceptance_criteria>
  <done>Full build + lint + unit suites green (no regressions; CLI local crawl still works); the local docker+embedded smoke produces a completed crawl with pages>0 and a 3-tier table with non-zero, tier-distinct cost/page; the result + re-run command are recorded in hardware-sizing.md. EXEC-04 + EXEC-06 met locally.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: LIVE Render re-verify — clear the 19-05 caveat (EXEC-05)</name>
  <read_first>
    - docs/runbooks/render-deploy.md (the existing render deploy + sync procedure)
    - .planning/phases/19.1-hosted-execution-path/19.1-CONTEXT.md (EXEC-05 live re-verify notes: service id, Firecrawl requirement, Blueprint-Sync ≠ deploy)
  </read_first>
  <what-built>
    The hosted execution path is now wired (Plans 01–03): startWorker builds WorkerDeps, per-job LLM config is honored, and LLM usage records to llm_usage. The local docker+embedded smoke (Task 2) already proved a real crawl completes with non-zero tier-distinct cost. This checkpoint re-verifies the SAME on the LIVE Render embedded-worker deploy to clear the 19-05/DEPLOY-02 caveat. It cannot be fully automated: it needs a real deploy (Blueprint Sync via the Render dashboard — the OSS repo has no auto-sync webhook), a crawler key (the distroless embedded worker has NO Playwright, so it MUST use Firecrawl), and a live LLM spend the user must consent to.
  </what-built>
  <action>
    First, automate everything possible:
    1. Ensure the fix is on the deploy branch: `git push` the merged 19.1 changes to the `render-paid-demo` branch (or document the exact branch the user must sync). Confirm render.yaml documents `SPATULA_CRAWLER=firecrawl` for the worker (Plan 01 already requires it for distroless). If render.yaml lacks `SPATULA_CRAWLER`/`FIRECRAWL_API_KEY` envVars (sync: false for the secret), ADD `SPATULA_CRAWLER: firecrawl` to the api service env and a `FIRECRAWL_API_KEY` (sync:false) entry, and note it in docs/runbooks/render-deploy.md.
    2. Update `docs/runbooks/render-deploy.md` with the EXEC-05 re-verify procedure (the steps below) so it is reproducible.
    Then hand off the genuinely-manual steps to the user.
  </action>
  <how-to-verify>
    USER STEPS (Render dashboard + a terminal):
    1. In the Render dashboard, set on the API service (srv-d8lh2q6rnols73dedvog): `OPENROUTER_API_KEY`, `SPATULA_CRAWLER=firecrawl`, `FIRECRAWL_API_KEY=<your key>` (the distroless embedded worker has no Playwright → Firecrawl is required).
    2. Blueprint Sync the `render-paid-demo` branch (render.yaml edits require a dashboard Blueprint Sync — `render deploys create` reuses stored config; CLI `blueprints` only validates). Wait for the deploy to go live and `/health` + `/health/ready` to return 200.
    3. Submit a small crawl and confirm it completes with pages + non-zero cost:
       `SPATULA_LIVE_LLM=1 SIZING_PAGES=3 SIZING_MAX_DEPTH=5 SIZING_CRAWLER=firecrawl SPATULA_API_URL=https://spatula-api.onrender.com pnpm sizing:baseline`
       (or submit one job manually via POST /api/v1/jobs + /start and poll GET /api/v1/jobs/:id).
    4. Confirm: the job reaches `completed` with `stats.pagesCompleted > 0`, AND `GET https://spatula-api.onrender.com/api/v1/usage?period=1d` shows non-zero `totalCostUsd` with a `byJob` entry for the crawl. This is the proof the embedded worker PROCESSED a crawl (not just `/health`) — clearing the 19-05 caveat.
    5. Paste the result (job status + pages + /usage totalCostUsd) back here.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -qi "SPATULA_CRAWLER\|firecrawl" docs/runbooks/render-deploy.md` (exit 0 — distroless→Firecrawl requirement documented)
    - `grep -qi "EXEC-05\|re-verif\|completed.*pages\|/usage" docs/runbooks/render-deploy.md` (exit 0 — live re-verify procedure documented)
    - User confirms (pasted evidence): a Render-processed crawl reached `completed`, pages > 0, and GET /api/v1/usage totalCostUsd > 0 — recorded in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Paste the live Render result (job status, pagesCompleted, /usage totalCostUsd) and type "verified" — or describe what failed (e.g. Firecrawl key, sync, crawl error) so we can iterate.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` + `pnpm lint` + queue/core/cli/api unit suites all green (no regressions; CLI local crawl path intact).
- worker-entrypoint.test.ts green (deps non-null + ALS attribution + per-job model).
- Local smoke: scripts/.sizing-results.json has 3 tiers, all cost/page > 0, ≥2 distinct (EXEC-06 harness green; EXEC-04 crawl completes with pages>0).
- hardware-sizing.md records the smoke result + re-run command; render-deploy.md records the EXEC-05 live procedure (Firecrawl requirement).
- Live Render crawl verified by the user: completed + pages>0 + non-zero /usage cost (EXEC-05; 19-05 caveat cleared).
</verification>

<success_criteria>
EXEC-04: a crawl submitted via the API completes with pages>0 + extractions; GET /api/v1/jobs/:id exposes stats.pagesCompleted; CLI local crawl unregressed.
EXEC-05: DEPLOY-02 re-verified LIVE on Render — a real crawl completes (pages + non-zero /usage cost) — 19-05 caveat cleared.
EXEC-06: DEPLOY-09 unblocked — pnpm sizing:baseline yields a per-tier table with non-zero, tier-distinct cost/page. "Run 19-05 until green" satisfied: local smoke green AND live Render crawl verified.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-hosted-execution-path/19.1-04-SUMMARY.md` documenting: the unit-test coverage, the local smoke result (3-row table, cost/page values), the no-regression gate outcome, and the live Render re-verify evidence (job status + pages + /usage cost). Note explicitly that the 19-05/DEPLOY-02 caveat is cleared and DEPLOY-09's harness is green (filling the CX32 1k table remains DEPLOY-09's separate live-run checkpoint).
</output>

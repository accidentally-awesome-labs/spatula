---
phase: 18-security-hardening-legal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/HISTORICAL_CONTRIBUTORS.md
  - docs/legal/uspto-tess-search.md
  - LICENSE
  - TRADEMARK.md
  - brand/LICENSE-BRAND.md
  - SECURITY.md
  - .github/CLA.md
  - CONTRIBUTING.md
  - README.md
  - packages/core/src/crawlers/playwright-crawler.ts
  - packages/core/src/crawlers/crawler-defaults.ts
  - packages/core/src/crawlers/crawler-defaults.test.ts
  - packages/core/src/index.ts
autonomous: false
requirements: [LEGAL-01, LEGAL-02, LEGAL-03, LEGAL-05, LEGAL-06, LEGAL-07, LEGAL-08]

must_haves:
  truths:
    - "A USPTO TESS search for 'Spatula' is run and documented BEFORE TRADEMARK.md is finalized"
    - "LICENSE copyright line reads 'Copyright (c) 2026 Accidentally Awesome Labs'"
    - "TRADEMARK.md defines the trademark policy naming Accidentally Awesome Labs as holder"
    - "brand/LICENSE-BRAND.md states brand assets are NOT under MIT — all rights reserved"
    - "SECURITY.md documents disclosure process, a GPG key, and a response SLA"
    - ".github/CLA.md is versioned (version frontmatter) and CONTRIBUTING.md documents the re-sign-on-change policy"
    - "README has a prominent legal disclaimer banner (MIT, target-site ToS responsibility, robots.txt honored)"
    - "The default crawler User-Agent is Spatula/<version> (+https://spatula.dev/abuse)"
    - ".github/HISTORICAL_CONTRIBUTORS.md enumerates the sole copyright holder"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license with correct copyright holder"
      contains: "Accidentally Awesome Labs"
    - path: "TRADEMARK.md"
      provides: "Trademark policy"
      contains: "Accidentally Awesome Labs"
    - path: "brand/LICENSE-BRAND.md"
      provides: "Brand-asset license carve-out"
      contains: "All rights reserved"
    - path: ".github/CLA.md"
      provides: "Versioned contributor license agreement text"
      contains: "version:"
    - path: "docs/legal/uspto-tess-search.md"
      provides: "Documented USPTO TESS trademark search result"
    - path: "packages/core/src/crawlers/crawler-defaults.ts"
      provides: "Default User-Agent string builder"
      contains: "spatula.dev/abuse"
  key_links:
    - from: "packages/core/src/crawlers/playwright-crawler.ts"
      to: "packages/core/src/crawlers/crawler-defaults.ts"
      via: "default User-Agent applied when options.userAgent is absent"
      pattern: "DEFAULT_USER_AGENT|buildUserAgent"
    - from: "CONTRIBUTING.md"
      to: ".github/CLA.md"
      via: "CLA section referencing the versioned CLA"
      pattern: "CLA"
---

<objective>
Ship the full legal docset that lets the repo flip public without lingering ambiguity:
run + document the USPTO TESS search, set the LICENSE copyright line, create TRADEMARK.md
+ brand license + versioned CLA, audit SECURITY.md, add the README legal banner, and set
the abuse-contact default User-Agent.

Purpose: Three pre-phase blockers (BLOCK-02 entity, BLOCK-06 trademark, BLOCK-09 historical
contributors) and seven LEGAL requirements must all be cleared before launch. A surfaced
trademark conflict is the one thing that can force a rename, so the TESS search is gated
FIRST via a checkpoint.
Output: HISTORICAL_CONTRIBUTORS.md, uspto-tess-search.md, LICENSE, TRADEMARK.md,
brand/LICENSE-BRAND.md, SECURITY.md, .github/CLA.md, CONTRIBUTING.md, README.md, and the
crawler default User-Agent.

NOTE: This plan is NOT autonomous — Task 2 contains a checkpoint:human-verify gate for the
USPTO TESS search result before TRADEMARK.md is written (D-02), and Task 5 contains a
checkpoint:human-action for installing the cla-assistant.io GitHub App (no CLI exists).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-security-hardening-legal/18-CONTEXT.md
@.planning/phases/18-security-hardening-legal/18-RESEARCH.md

# Spec — legal section (LICENSE / TRADEMARK / brand / CLA / disclaimer / User-Agent)
@docs/superpowers/specs/2026-04-20-wave-6-phase-14-public-launch-design.md

<interfaces>
<!-- Crawler User-Agent integration point — extracted from packages/core/src/crawlers/playwright-crawler.ts -->
```typescript
// playwright-crawler.ts CURRENT: contextOptions.userAgent is set ONLY when options?.userAgent
// is provided. There is NO default. LEGAL-08 adds a default when the option is absent.
if (options?.userAgent) { contextOptions.userAgent = options.userAgent; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HISTORICAL_CONTRIBUTORS.md + LICENSE copyright line</name>
  <files>.github/HISTORICAL_CONTRIBUTORS.md, LICENSE</files>
  <read_first>
    - LICENSE (the file being modified — current copyright line reads "Spatula Contributors")
    - .planning/phases/18-security-hardening-legal/18-CONTEXT.md (D-01 — entity formed; D-03 — solo contributor)
  </read_first>
  <action>
    Run `git log --format='%ae' | sort -u` to confirm the contributor set. CONTEXT.md D-03
    states it is a single author (`salar.sayyad@gmail.com`).

    Create `.github/HISTORICAL_CONTRIBUTORS.md` — a short doc (BLOCK-09) with a one-line
    enumeration of the sole copyright holder: the author name + the confirmed email, plus
    a sentence stating that `git log --format='%ae' | sort -u` confirms a single author and
    that no third-party pre-sign outreach is required (D-03). Include the actual command
    output as evidence.

    Modify `LICENSE` — change the copyright line to exactly:
    `Copyright (c) 2026 Accidentally Awesome Labs`
    Per D-01 the legal entity IS formed: no interim-name fallback, no `NOTICE.md`, no
    assignment commit. Leave the rest of the MIT license body unchanged.
  </action>
  <verify>
    <automated>grep -q "Copyright (c) 2026 Accidentally Awesome Labs" LICENSE && test -s .github/HISTORICAL_CONTRIBUTORS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Copyright (c) 2026 Accidentally Awesome Labs" LICENSE` succeeds
    - `grep -vq "Spatula Contributors" LICENSE` — old copyright line removed
    - `.github/HISTORICAL_CONTRIBUTORS.md` exists, is non-empty, and contains the sole-author email
    - LICENSE still contains the MIT permission text (`Permission is hereby granted`)
  </acceptance_criteria>
  <done>LICENSE copyright line reads "Copyright (c) 2026 Accidentally Awesome Labs" with the MIT body intact; HISTORICAL_CONTRIBUTORS.md enumerates the sole author with git-log evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run USPTO TESS search, document it, then write TRADEMARK.md + brand/LICENSE-BRAND.md</name>
  <files>docs/legal/uspto-tess-search.md, TRADEMARK.md, brand/LICENSE-BRAND.md</files>
  <read_first>
    - LICENSE (Task 1 — entity name to reference as the trademark holder)
    - docs/superpowers/specs/2026-04-20-wave-6-phase-14-public-launch-design.md §3.9 (trademark policy spec — fork naming rules, "based on Spatula" attribution, unmodified-release exemption)
    - .planning/phases/18-security-hardening-legal/18-CONTEXT.md (D-02 — search-then-write ordering; surfaced conflict escalates)
  </read_first>
  <action>
    STEP A — Run the USPTO TESS search. Use the WebFetch tool against the USPTO trademark
    search system (https://tmsearch.uspto.gov/ — the modern TESS replacement) querying the
    term "Spatula" in the relevant goods/services classes (software / SaaS — IC 009 and IC 042).
    Capture the result.

    STEP B — Create `docs/legal/uspto-tess-search.md` documenting: the search date, the
    search system used, the exact query terms + classes, the result (list any live marks
    found for "Spatula" in IC 009 / IC 042, or state "no conflicting live marks found"),
    and a conclusion line — either "conflict-free, proceed" or "POTENTIAL CONFLICT — escalate".

    STEP C — STOP at the checkpoint task that follows (Task 2 checkpoint). Do NOT write
    TRADEMARK.md or brand/LICENSE-BRAND.md until the user confirms the search is conflict-free
    (D-02: search-then-write is ordered; a surfaced conflict escalates to a rename, which is
    out of scope for autonomous execution).

    STEP D (after the checkpoint is approved) — Create `TRADEMARK.md` per spec §3.9: name
    "Accidentally Awesome Labs" as the trademark holder; state that forks may NOT use the
    "Spatula" name or logo in their project name, domain, or marketing; state that "based on
    Spatula" attribution IS permitted; state that an unmodified official release MAY use the
    name. Use Apache-style trademark-policy phrasing.

    STEP E — Create `brand/LICENSE-BRAND.md` with the exact text from CONTEXT.md Specifics:
    a clear statement that brand assets (logo, wordmark, icons) are NOT covered by the MIT
    LICENSE — "All rights reserved. Use per TRADEMARK.md." Expand slightly: name the
    `brand/` directory as the covered scope and cross-reference `TRADEMARK.md`.
  </action>
  <verify>
    <automated>test -s docs/legal/uspto-tess-search.md && grep -q "Accidentally Awesome Labs" TRADEMARK.md && grep -q "All rights reserved" brand/LICENSE-BRAND.md && grep -q "TRADEMARK.md" brand/LICENSE-BRAND.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/legal/uspto-tess-search.md` exists, is non-empty, and contains a search date + a conclusion line
    - `TRADEMARK.md` exists and contains `Accidentally Awesome Labs`, the words `based on Spatula`, and a fork-naming restriction
    - `brand/LICENSE-BRAND.md` exists and contains both `All rights reserved` and a reference to `TRADEMARK.md`
    - the checkpoint was reached and resolved (user confirmed conflict-free OR escalation logged)
  </acceptance_criteria>
  <done>USPTO TESS search run + documented in uspto-tess-search.md; user confirmed the result via checkpoint; TRADEMARK.md (Accidentally Awesome Labs holder, fork rules, attribution + unmodified exemptions) and brand/LICENSE-BRAND.md ("All rights reserved. Use per TRADEMARK.md.") written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 checkpoint: Confirm USPTO TESS search result before TRADEMARK.md is finalized</name>
  <files>docs/legal/uspto-tess-search.md</files>
  <what-built>
    A USPTO trademark search for "Spatula" (IC 009 / IC 042 — software / SaaS) was run and
    documented in docs/legal/uspto-tess-search.md. The search either found no conflicting
    live marks (proceed) or surfaced a potential conflict (escalate to rename).
  </what-built>
  <how-to-verify>
    1. Open docs/legal/uspto-tess-search.md and review the documented search result.
    2. Independently spot-check at https://tmsearch.uspto.gov/ — search "Spatula", filter to
       software / SaaS goods-and-services classes.
    3. Confirm there is NO live registered or pending mark for "Spatula" that conflicts with
       a software/data-tooling product.
  </how-to-verify>
  <action>
    Pause execution and present docs/legal/uspto-tess-search.md to the user. Wait for the
    user's verdict on the trademark-conflict question before Task 2 STEP D/E runs (D-02 —
    search-then-write is ordered). Do NOT write TRADEMARK.md or brand/LICENSE-BRAND.md until
    the user confirms "conflict-free". If the user reports a conflict, stop and surface it
    as a BLOCK-06 escalation — a rename is out of scope for autonomous execution and needs a
    separate planning session.
  </action>
  <verify>User reviews docs/legal/uspto-tess-search.md and confirms the result is conflict-free (or surfaces a conflict for escalation).</verify>
  <done>The user has reviewed the documented TESS search and either confirmed "conflict-free" (Task 2 proceeds to write TRADEMARK.md + brand/LICENSE-BRAND.md) or surfaced a conflict logged as a BLOCK-06 escalation.</done>
  <resume-signal>
    Type "conflict-free" to proceed with writing TRADEMARK.md naming Accidentally Awesome
    Labs as holder, OR describe the conflict found so the rename path can be escalated to a
    separate planning session (BLOCK-06 escalation).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: SECURITY.md audit — disclosure process, GPG key, response SLA</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (the file being modified — current state; RESEARCH notes it has a security@spatula.dev email and no GPG key)
    - docs/superpowers/specs/2026-04-20-wave-6-phase-14-public-launch-design.md §3.9 (SECURITY.md requirements)
  </read_first>
  <action>
    Audit and update `SECURITY.md` (LEGAL-05). Ensure it documents, as named sections:
    - Supported versions — a table of which version lines receive security fixes.
    - Reporting a vulnerability — the disclosure process: the security contact
      (`security@spatula.dev`), a preference for private disclosure, and a request NOT to
      open public issues for vulnerabilities.
    - GPG key — a "GPG Key" section. The actual key fingerprint/block is operator-provided;
      add a clearly-marked placeholder block (`<!-- GPG public key block — operator fills before launch -->`)
      with a fingerprint placeholder, AND a note in the SUMMARY that the operator must paste
      the real key before the public flip. The section MUST exist now so the audit gate passes.
    - Response SLA — a "Response SLA" section: acknowledge within X business days, triage
      within Y, with a Critical-issue 24h target (consistent with the incident-response SLA
      referenced in Phase 22 LAUNCH-05).
  </action>
  <verify>
    <automated>grep -qi "GPG" SECURITY.md && grep -qi "SLA" SECURITY.md && grep -qi "vulnerabilit" SECURITY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi "GPG" SECURITY.md` succeeds (GPG Key section present)
    - `grep -qi "SLA" SECURITY.md` succeeds (Response SLA section present)
    - `grep -qi "report" SECURITY.md` succeeds (reporting/disclosure process present)
    - SECURITY.md contains a "Supported versions" section or table
  </acceptance_criteria>
  <done>SECURITY.md has Supported-versions, Reporting-a-vulnerability (private disclosure + contact), GPG-Key (placeholder block flagged for operator fill), and Response-SLA (Critical 24h) sections.</done>
</task>

<task type="auto">
  <name>Task 4: README legal disclaimer banner + default User-Agent</name>
  <files>README.md, packages/core/src/crawlers/playwright-crawler.ts, packages/core/src/crawlers/crawler-defaults.ts, packages/core/src/crawlers/crawler-defaults.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - README.md (the file being modified — find where to insert the banner, near the top)
    - packages/core/src/crawlers/playwright-crawler.ts (the file being modified — current userAgent handling at the contextOptions block)
    - packages/core/src/crawlers/robots-txt.ts (other place User-Agent is referenced — keep consistent)
    - packages/core/src/index.ts (barrel — export the new defaults if other crawlers need them)
    - package.json (root — to confirm the version string source for User-Agent)
  </read_first>
  <action>
    PART A — README legal disclaimer banner (LEGAL-07). Insert a clearly-set-off banner
    section near the top of `README.md` (right after the project title/tagline, before
    installation). It must state, as a blockquote or fenced callout:
    - Spatula is MIT-licensed.
    - Users are responsible for complying with the Terms of Service of any site they crawl.
    - Spatula honors `robots.txt` by default; overriding that is at the user's own risk.

    PART B — Default User-Agent (LEGAL-08). Create
    `packages/core/src/crawlers/crawler-defaults.ts` exporting a `buildUserAgent(version: string)`
    function and a `DEFAULT_USER_AGENT` constant (or a function called with the package
    version) producing exactly: `Spatula/<version> (+https://spatula.dev/abuse)`.
    Source `<version>` from the `@spatula/core` package.json `version` field (import it, or
    read it via a small version helper if one already exists — check
    `packages/core/src/` for an existing version export first; reuse it if present).

    Modify `packages/core/src/crawlers/playwright-crawler.ts`: change the userAgent block so
    when `options?.userAgent` is ABSENT, `contextOptions.userAgent` falls back to the default:
    ```typescript
    contextOptions.userAgent = options?.userAgent ?? DEFAULT_USER_AGENT;
    ```
    Export `buildUserAgent` / `DEFAULT_USER_AGENT` from `packages/core/src/index.ts` so the
    Firecrawl crawler and robots-txt checks can reuse the same string.

    Write `crawler-defaults.test.ts` (co-located, unit): assert `buildUserAgent('1.2.3')`
    returns exactly `Spatula/1.2.3 (+https://spatula.dev/abuse)` and that `DEFAULT_USER_AGENT`
    matches the `Spatula/<semver> (+https://spatula.dev/abuse)` shape.
  </action>
  <verify>
    <automated>pnpm --filter @spatula/core run test -- crawler-defaults && grep -qi "robots.txt" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi "robots.txt" README.md` succeeds and the banner also mentions MIT + ToS responsibility
    - `packages/core/src/crawlers/crawler-defaults.ts` exists and contains the literal `spatula.dev/abuse`
    - `grep -q "DEFAULT_USER_AGENT\|buildUserAgent" packages/core/src/crawlers/playwright-crawler.ts` succeeds
    - `grep -q "buildUserAgent\|DEFAULT_USER_AGENT" packages/core/src/index.ts` succeeds (barrel export)
    - `pnpm --filter @spatula/core run test -- crawler-defaults` exits 0
    - the test asserts the exact string `Spatula/1.2.3 (+https://spatula.dev/abuse)` for input `1.2.3`
  </acceptance_criteria>
  <done>README has a top-of-file legal banner (MIT, ToS responsibility, robots.txt default + override-at-risk); crawler-defaults.ts builds `Spatula/<version> (+https://spatula.dev/abuse)`; playwright-crawler falls back to it when no userAgent option is given; crawler-defaults.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 5: Versioned CLA + CONTRIBUTING.md re-sign policy</name>
  <files>.github/CLA.md, CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (the file being modified — current state; find where a CLA section fits)
    - .planning/phases/18-security-hardening-legal/18-RESEARCH.md (Pattern 9 — cla-assistant.io wiring; CLA.md version frontmatter)
    - docs/superpowers/specs/2026-04-20-wave-6-phase-14-public-launch-design.md §3.9 (CLA requirement)
  </read_first>
  <action>
    Create `.github/CLA.md` (LEGAL-06). It MUST start with YAML frontmatter containing a
    `version` field:
    ```
    ---
    version: 1
    ---
    ```
    Below the frontmatter, write a standard individual Contributor License Agreement: the
    contributor grants Accidentally Awesome Labs a license to their contributions, retains
    their own copyright, and warrants the contribution is their own work. Keep it concise
    and apache-CLA-style.

    Modify `CONTRIBUTING.md`: add a "Contributor License Agreement" section that — references
    `.github/CLA.md`, states that all contributors must sign via the cla-assistant.io bot on
    their first PR, and documents the re-sign-on-text-change policy: when the CLA `version`
    in the frontmatter is bumped, past signatories must re-sign on their next PR
    (cla-assistant auto-detects the text change).

    After writing both files, STOP at the checkpoint task that follows (Task 5 checkpoint) —
    the cla-assistant.io GitHub App must be installed on the repo by the human; there is no
    CLI for installing a GitHub App.
  </action>
  <verify>
    <automated>head -5 .github/CLA.md | grep -q "version:" && grep -qi "CLA" CONTRIBUTING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/CLA.md` exists and its YAML frontmatter (within the first 5 lines) contains `version:`
    - `.github/CLA.md` body contains the words "Contributor License Agreement" or "CLA" and grants a license to Accidentally Awesome Labs
    - `grep -qi "re-sign\|resign" CONTRIBUTING.md` succeeds (re-sign policy documented)
    - `grep -qi "cla-assistant" CONTRIBUTING.md` succeeds
  </acceptance_criteria>
  <done>.github/CLA.md has version:1 frontmatter + an individual CLA body; CONTRIBUTING.md has a CLA section referencing it, the cla-assistant.io bot, and the re-sign-on-version-bump policy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5 checkpoint: Install the cla-assistant.io GitHub App</name>
  <files>.github/CLA.md</files>
  <what-built>
    .github/CLA.md (versioned CLA text) and the CONTRIBUTING.md CLA section are committed.
    The cla-assistant.io GitHub App now needs to be installed on the repo so it comments on
    PRs and records signatures — there is no CLI or API for installing a GitHub App.
  </what-built>
  <how-to-verify>
    1. Go to https://cla-assistant.io and sign in with the GitHub account that owns the repo.
    2. Configure cla-assistant for the Spatula repository.
    3. Point it at .github/CLA.md as the CLA text source.
    4. Confirm the cla-assistant GitHub App is installed and shows the repo in its dashboard.
  </how-to-verify>
  <action>
    Pause execution and present the cla-assistant.io install steps to the user. There is no
    CLI or API to install a GitHub App, so this step is human-only. Wait for the user to
    confirm the app is installed and pointed at .github/CLA.md, or to defer it. If deferred,
    record it in the SUMMARY as a pre-launch follow-up that Phase 22's launch gate must
    re-check.
  </action>
  <verify>User confirms the cla-assistant.io GitHub App is installed on the repo and configured against .github/CLA.md (or explicitly defers it).</verify>
  <done>The cla-assistant.io GitHub App is installed on the repo and configured to use .github/CLA.md, OR the user deferred it and the SUMMARY records it as a Phase 22 launch-gate follow-up.</done>
  <resume-signal>
    Type "cla-installed" once the cla-assistant.io app is configured against the repo and
    .github/CLA.md, or "defer" to record it as a pre-launch follow-up (BLOCK / Phase 22 gate).
  </resume-signal>
</task>

</tasks>

<verification>
- USPTO TESS search documented; user confirmed conflict-free before TRADEMARK.md written
- LICENSE copyright line + TRADEMARK.md + brand/LICENSE-BRAND.md + .github/CLA.md all present and correct
- SECURITY.md has GPG + SLA + reporting sections
- README has the legal banner; default User-Agent is Spatula/<version> (+https://spatula.dev/abuse)
- `pnpm --filter @spatula/core run test -- crawler-defaults` passes
</verification>

<success_criteria>
- LEGAL-01: LICENSE copyright line reads "Copyright (c) 2026 Accidentally Awesome Labs" (D-01, BLOCK-02 entity formed)
- LEGAL-02: TRADEMARK.md defines the trademark policy (BLOCK-06 — TESS search run + documented first per D-02)
- LEGAL-03: brand/LICENSE-BRAND.md states brand assets are NOT under MIT
- LEGAL-05: SECURITY.md audited — disclosure process, GPG key section, response SLA
- LEGAL-06: .github/CLA.md versioned + CONTRIBUTING.md re-sign policy + cla-assistant.io wired
- LEGAL-07: README legal disclaimer banner (MIT, ToS responsibility, robots.txt default)
- LEGAL-08: Default User-Agent is Spatula/<version> (+https://spatula.dev/abuse)
- BLOCK-09: .github/HISTORICAL_CONTRIBUTORS.md committed (D-03 — solo contributor)
</success_criteria>

<output>
After completion, create `.planning/phases/18-security-hardening-legal/18-04-SUMMARY.md`
</output>

import { describe, it } from 'vitest';

// WorkerDeps is a pure data bag — constructor stores injected deps.
// TypeScript enforces the shape; no runtime logic to test.
describe('WorkerDeps', () => {
  it.skip('no runtime logic to test', () => {});
});

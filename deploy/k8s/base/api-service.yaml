apiVersion: v1
kind: Service
metadata:
  name: spatula-api
  namespace: spatula
  labels:
    app: spatula-api
spec:
  type: ClusterIP
  selector:
    app: spatula-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
